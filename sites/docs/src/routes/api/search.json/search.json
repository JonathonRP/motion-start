[{"title":"Examples","href":"/docs/examples","description":"A gallery of everything Motion Start can do, in one scroll.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import TransitionDemo from \"$lib/components/demos/transition-demo.svelte\";\r  import StaggerDemo from \"$lib/components/demos/stagger-demo.svelte\";\r  import GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r  import DragDemo from \"$lib/components/demos/drag-demo.svelte\";\r  import LayoutDemo from \"$lib/components/demos/layout-demo.svelte\";\r  import SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\r  import PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\r  import ReorderDemo from \"$lib/components/demos/reorder-demo.svelte\";\r  import MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\r \r \r Every demo below is live — the same components used throughout the rest of these docs, collected\r in one place. Each links to the page that explains it.\r Transitions\r \r Springs are the default for transforms. Everything else eases.\r \r \r  \r \r \r `svelte\r \r `\r \r Read more in /docs/animation/transitions.\r Variants and stagger\r \r Variants let a parent orchestrate its children, so a list can cascade without any per-item timing.\r \r \r  \r \r \r `svelte\r \r  {#each items as item (item)}\r   \r  {/each}\r \r `\r \r Read more in /docs/animation/overview.\r Gestures\r \r whileHover and whileTap are transient states — release the gesture and the element returns to\r whatever animate says, with no bookkeeping.\r \r \r  \r \r \r `svelte\r \r `\r Drag\r \r \r  \r \r \r `svelte\r \r `\r \r Read more in /docs/animation/gestures.\r Layout animations\r \r layout measures the element before and after a change and animates the difference with\r transforms, so you can animate properties that are otherwise far too expensive.\r \r \r  \r \r \r `svelte\r \r `\r Shared layout\r \r A layoutId moves a single element between positions — the classic animated tab indicator.\r \r \r  \r \r \r `svelte\r \r `\r \r Read more in /docs/animation/layout.\r Exit animations\r \r AnimatePresence keeps an element in the DOM long enough to animate out.\r \r \r  \r \r \r `svelte\r \r  {#if visible}\r   \r  {/if}\r \r `\r \r Read more in /docs/components/animate-presence.\r Reorder\r \r \r  \r \r \r `svelte\r \r  {#each items as item (item)}\r   \r  {/each}\r \r `\r \r Read more in /docs/components/reorder.\r Motion values\r \r A motion value updates outside the component lifecycle, so you can drive an animation every frame\r without re-rendering.\r \r \r  \r \r \r `svelte\r const x = useMotionValue(0);\r const opacity = useTransform(x, /docs/motion-values/overview.\r \r \r Every demo on this page is a plain Svelte component in\r src/lib/components/demos — copy one and start editing.\r "},{"title":"Getting Started","href":"/docs/getting-started","description":"Install Motion Start and write your first animation.","content":"\r  import { Callout, Steps, DemoContainer } from \"@svecodocs/kit\";\r  import PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\r Requirements\r \r Motion Start requires Svelte 5. Runes are used throughout the library, so a Svelte 4 project\r will need to migrate first.\r Install\r \r `bash\r npm install motion-start\r `\r \r While the rewrite is in alpha, install the next tag explicitly:\r \r `bash\r npm install motion-start@next\r `\r Import\r \r Everything is exported from the package root:\r \r `ts\r import { motion, AnimatePresence, useMotionValue } from \"motion-start\";\r `\r \r Smaller entry points are available when you only need part of the library:\r \r | Entry                    | Contents                                                    |\r | ------------------------ | ----------------------------------------------------------- |\r | motion-start           | Everything — components, hooks, values, projection.          |\r | motion-start/m         | The m component only, for use with LazyMotion.           |\r | motion-start/dom       | The imperative DOM API — animate, scroll, inView.      |\r | motion-start/mini      | The mini animation API, backed by WAAPI.                     |\r | motion-start/dom/mini  | The mini DOM API.                                            |\r | motion-start/projection | The projection engine, for advanced layout work.            |\r Your first animation\r \r Any HTML or SVG element is available on the motion proxy. Give it an animate prop and it will\r animate to that state whenever the values change.\r \r `svelte\r \r  import { motion } from \"motion-start\";\r \r \r \r `\r \r initial — the state the element mounts in.\r animate — the state to animate towards.\r transition — how to get there.\r Animating elements out\r \r Svelte removes elements from the DOM immediately, so exit animations need AnimatePresence to keep\r them alive until the animation finishes.\r \r \r  \r \r \r `svelte\r \r  import { motion, AnimatePresence } from \"motion-start\";\r \r  let visible = $state(true);\r \r \r \r  {#if visible}\r   \r  {/if}\r \r `\r \r \r \r Inside {#each} blocks, always use a keyed block ({#each items as item (item.id)}) so\r AnimatePresence can tell which element is leaving.\r Next steps\r \r /docs/animation/overview — variants, keyframes and orchestration.\r /docs/animation/gestures — hover, press, drag and viewport.\r /docs/animation/layout — the layout prop and shared elements.\r /docs/motion-values/overview — composing values outside of Svelte state."},{"title":"Introduction","href":"/docs/index","description":"A production-ready declarative motion library for Svelte.","content":"\r  import { Callout, Card, CardGrid, DemoContainer } from \"@svecodocs/kit\";\r  import GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r \r \r Motion Start is a declarative animation library for Svelte 5, built as a port of\r https://motion.dev 11.11.11. You describe the state you want an element to be in\r and Motion Start works out how to get there — including layout changes, gestures, scroll, and\r enter/exit animations.\r \r \r  \r \r \r Every animation above is just a prop:\r \r `svelte\r \r  import { motion } from \"motion-start\";\r \r \r \r `\r \r \r \r Motion Start is currently in alpha. APIs may still change and you should expect bugs — please\r https://github.com/JonathonRP/motion-start/issues or open a PR.\r What's new in 0.2\r \r 0.2.0-next.0 is a rewrite against framer-motion@11.11.11 (previously 4.0.3) on Svelte 5 runes.\r \r New\r \r Reorder.Group / Reorder.Item for drag-to-reorder lists.\r LayoutGroup and the full projection engine, replacing AnimateSharedLayout.\r AnimatePresence mode (sync | wait | popLayout) and usePresenceData.\r WAAPI-accelerated animations, animate() sequences, and stagger().\r Scroll (useScroll), viewport (whileInView) and useInView support.\r Motion value hooks: useTime, useVelocity, useWillChange, useAnimate, useAnimateMini.\r Subpath entries: motion-start/dom, /mini, /m, /client, /projection.\r \r Changed\r \r Components and hooks are runes-based; the renderless Use*.svelte wrappers are gone in favour of\r   plain functions and classes.\r motion and m are proxies, so any element is available as motion..\r Gestures are rewritten onto the shared Feature pipeline (hover, press, pan, drag, focus).\r \r Removed\r \r AnimateSharedLayout — use LayoutGroup with layoutId.\r MotionDiv — use motion.div.\r Explore\r \r \r  \r   Install Motion Start and write your first animation.\r  \r  \r   Props, variants, keyframes and orchestration.\r  \r  \r   Hover, press, pan, drag and viewport.\r  \r  \r   Animate between any two layouts with a single prop.\r  \r  \r   motion, AnimatePresence, LayoutGroup, Reorder and more.\r  \r  \r   Track, transform and compose values outside of Svelte state.\r  \r "},{"title":"Overview","href":"/docs/animation/overview","description":"How animations are described in Motion Start.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import StaggerDemo from \"$lib/components/demos/stagger-demo.svelte\";\r \r \r Motion Start scales from simple prop-based animations up to full orchestration. Animations are\r declarative: you set a target and the library animates from wherever the value currently is.\r Animating props\r \r Any value in animate is animated whenever it changes.\r \r `svelte\r \r `\r \r initial defines the mount state. Set initial={false} to skip the mount animation and start at\r the animate values.\r \r `svelte\r \r \r `\r Transitions\r \r transition controls how a value animates. Numeric and transform values default to a spring;\r everything else defaults to a tween.\r \r `svelte\r \r \r `\r \r Per-value transitions are supported too:\r \r `svelte\r \r `\r \r See /docs/animation/transitions for every option.\r Keyframes\r \r Pass an array to animate through a series of values. The first entry can be null to start from the\r current value.\r \r `svelte\r \r `\r Variants\r \r Variants are named animation targets. Naming them lets a parent orchestrate its children — a child\r automatically inherits the variant label of its parent unless it defines its own.\r \r \r  \r \r \r `svelte\r \r  import { motion } from \"motion-start\";\r \r  const container = {\r   hidden: { opacity: 0 },\r   visible: {\r    opacity: 1,\r    transition: { staggerChildren: 0.08, delayChildren: 0.1 }\r   }\r  };\r \r  const item = {\r   hidden: { opacity: 0, y: 16 },\r   visible: { opacity: 1, y: 0 }\r  };\r \r \r \r  {#each items as n (n)}\r   \r  {/each}\r \r `\r \r Orchestration options available on a parent transition:\r \r | Option            | Description                                                        |\r | ----------------- | ------------------------------------------------------------------ |\r | staggerChildren | Delay between each child's animation, in seconds.                  |\r | staggerDirection | 1 to stagger forwards, -1 backwards.                          |\r | delayChildren   | Delay before the first child animates.                             |\r | when            | \"beforeChildren\" or \"afterChildren\".                           |\r Animation lifecycle\r \r `svelte\r  console.log(\"start\")}\r  onAnimationComplete={(definition) => console.log(\"done\", definition)}\r  onUpdate={(latest) => console.log(latest.x)}\r />\r `\r Reduced motion\r \r Respect the user's system preference with useReducedMotion, or configure it globally with\r /docs/components/motion-config.\r \r `svelte\r \r  import { useReducedMotion } from \"motion-start\";\r \r  const shouldReduceMotion = useReducedMotion();\r \r \r \r `\r \r \r \r Hooks that track reactive state — useReducedMotion, useInView — return a getter. Call it from\r reactive code (a $derived, a template expression) so the read stays tracked.\r \r \r \r \r \r For animations that aren't tied to a component's state, the DOM API is available from\r motion-start/dom: animate(), scroll(), inView() and stagger().\r \r "},{"title":"Layout animations","href":"/docs/animation/layout","description":"Animate between any two layouts, even between different elements.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import LayoutDemo from \"$lib/components/demos/layout-demo.svelte\";\r  import SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\r \r \r CSS layout properties like width, flex and grid are expensive to animate directly. Motion\r Start instead measures the element before and after the change and animates the difference using\r transforms — smooth, and cheap.\r \r It's a single prop.\r \r \r  \r \r \r `svelte\r \r `\r Layout options\r \r | Value              | Description                                                        |\r | ------------------ | ------------------------------------------------------------------ |\r | layout           | Animate both position and size.                                    |\r | layout=\"position\" | Animate position only — useful for text that shouldn't stretch.   |\r | layout=\"size\"    | Animate size only.                                                 |\r | layoutDependency  | Only measure when this value changes, instead of on every render.  |\r | layoutRoot       | Make this element the projection root for its subtree.             |\r | layoutScroll     | Mark a scrollable ancestor so scroll offsets are accounted for.    |\r Scale correction\r \r Because layout animations use scale, children can appear distorted. Give affected children their\r own layout prop to correct them, and prefer borderRadius and boxShadow as motion values or\r animated props so they're corrected automatically.\r \r `svelte\r \r  label\r \r `\r Shared layout animations\r \r Give two elements the same layoutId and Motion Start animates between them as the first unmounts\r and the second mounts.\r \r \r  \r \r \r `svelte\r {#each tabs as tab (tab)}\r   (selected = tab)}>\r   {#if selected === tab}\r    \r   {/if}\r   {tab}\r  \r {/each}\r `\r \r Wrap the group in /docs/components/layout-group when the shared elements live in\r different components, or when sibling components need to re-measure together.\r \r \r \r AnimateSharedLayout was removed in 0.2. Use LayoutGroup with layoutId instead — shared\r layout animations no longer require a wrapper unless you need grouping.\r Layout and presence\r \r Combine layout with /docs/components/animate-presence so surrounding items\r reflow while an element animates out. Use mode=\"popLayout\" to take the exiting element out of the\r flow immediately.\r \r `svelte\r \r  {#each items as item (item.id)}\r   \r  {/each}\r \r `\r Troubleshooting\r \r The element jumps. Its nearest scrollable ancestor probably needs layoutScroll.\r Nothing animates. Layout animations need a layout change caused by a re-render; a transform\r   change alone won't trigger one.\r Text stretches. Use layout=\"position\"."},{"title":"Gestures","href":"/docs/animation/gestures","description":"Hover, press, pan, drag and viewport detection, as animation props and event handlers.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r  import DragDemo from \"$lib/components/demos/drag-demo.svelte\";\r \r \r Motion Start extends Svelte's event listeners with a set of UI gestures. Each gesture has both a set\r of event handlers and a while- animation prop that applies a temporary animation target.\r Hover\r \r whileHover applies while a pointer is over the element. Unlike :hover, it only fires for actual\r mouse-like pointers, so it won't get stuck on touch devices.\r \r `svelte\r  {}}\r  onHoverEnd={(event, info) => {}}\r />\r `\r Press\r \r whileTap applies from pointer-down until pointer-up. onTap only fires when the pointer is\r released over the same element, so dragging away cancels it.\r \r `svelte\r  {}}\r  onTap={(event, info) => {}}\r  onTapCancel={(event, info) => {}}\r />\r `\r Focus\r \r whileFocus follows the :focus-visible rules — it applies on keyboard focus, not on click.\r \r `svelte\r \r `\r Pan\r \r Pan recognises a pointer moving across an element without moving the element itself.\r \r `svelte\r  {}}\r  onPan={(event, info) => console.log(info.offset.x, info.velocity.x)}\r  onPanEnd={(event, info) => {}}\r />\r `\r \r info is a PanInfo: point, delta, offset and velocity.\r Drag\r \r \r  \r \r \r `svelte\r \r `\r \r | Prop              | Description                                                                 |\r | ----------------- | --------------------------------------------------------------------------- |\r | drag            | true, \"x\" or \"y\".                                                      |\r | dragConstraints | Pixel bounds, or a ref to an element to stay within.                         |\r | dragElastic     | 0–1, how far past the constraints the element can be dragged.            |\r | dragMomentum    | Whether to continue with inertia on release. Defaults to true.             |\r | dragSnapToOrigin | Animate back to the origin on release.                                      |\r | dragControls    | A DragControls instance for starting drags from another element.           |\r | dragListener    | Set to false to only start drags via dragControls.                       |\r Drag controls\r \r Start a drag from a handle, or anywhere else:\r \r `svelte\r \r  import { motion, useDragControls } from \"motion-start\";\r \r  const controls = useDragControls();\r \r \r  controls.start(event, { snapToCursor: true })}>handle\r \r \r `\r Viewport\r \r whileInView animates when the element enters the viewport, and viewport configures detection.\r \r `svelte\r  {}}\r  onViewportLeave={(entry) => {}}\r />\r `\r \r | Option   | Description                                                            |\r | -------- | ---------------------------------------------------------------------- |\r | once   | Only animate the first time the element enters.                        |\r | amount | \"some\", \"all\", or a 0–1 threshold.                             |\r | margin | A root margin, e.g. \"0px 0px -100px 0px\".                            |\r | root   | A ref to a scrollable ancestor to use instead of the viewport.         |\r \r \r \r while- props are applied in order of priority: whileHover, whileTap, whileDrag,\r whileFocus, whileInView. A later gesture overrides values set by an earlier one, and the element\r animates back down the stack as gestures end.\r \r "},{"title":"Scroll","href":"/docs/animation/scroll","description":"Scroll-linked and scroll-triggered animations.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r \r \r There are two kinds of scroll animation. Scroll-linked animations are tied directly to scroll\r position, and scroll-triggered animations start when an element enters the viewport.\r Scroll-triggered\r \r Use whileInView. See /docs/animation/gestures#viewport for the full viewport options.\r \r `svelte\r \r `\r \r For non-visual work — lazy loading, analytics — use useInView with an element ref.\r \r `svelte\r \r  import { useInView } from \"motion-start\";\r \r  const ref = { current: null };\r  const isInView = useInView(ref, { once: true, amount: 0.5 });\r \r \r \r  {#if isInView()}\r   \r  {/if}\r \r `\r Scroll-linked\r \r useScroll returns four motion values that update as the page or a container scrolls.\r \r `svelte\r \r  import { motion, useScroll } from \"motion-start\";\r \r  const { scrollYProgress } = useScroll();\r \r \r \r `\r \r | Value             | Description                                        |\r | ----------------- | -------------------------------------------------- |\r | scrollX         | Horizontal scroll offset in pixels.                |\r | scrollY         | Vertical scroll offset in pixels.                  |\r | scrollXProgress | Horizontal progress, 0–1.                      |\r | scrollYProgress | Vertical progress, 0–1.                        |\r Tracking an element\r \r Pass a target ref to track a specific element, and offset to define when tracking starts and\r ends.\r \r `svelte\r \r  import { motion, useScroll, useTransform } from \"motion-start\";\r \r  const ref = { current: null };\r  const { scrollYProgress } = useScroll({\r   target: ref,\r   offset: [\"start end\", \"end start\"]\r  });\r  const opacity = useTransform(scrollYProgress, [0, 0.5, 1], [0, 1, 0]);\r \r \r \r  \r \r `\r \r useScroll also accepts a container ref to track a scrollable element instead of the window.\r \r \r \r useScroll expects a ref object ({ current: element }), not a bare element, so it can pick the\r element up once Svelte has bound it.\r Imperative scroll\r \r For animations outside of components, scroll() from motion-start/dom drives any animation with\r scroll progress:\r \r `ts\r import { animate, scroll } from \"motion-start/dom\";\r \r scroll(animate(\"#header\", { backgroundColor: [\"#fff\", \"#000\"] }, { ease: \"linear\" }));\r `"},{"title":"Transitions","href":"/docs/animation/transitions","description":"Springs, tweens, inertia and orchestration options.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import TransitionDemo from \"$lib/components/demos/transition-demo.svelte\";\r \r \r A transition describes how a value animates. Transforms and other numeric values default to a\r spring; colours, opacity and non-physical values default to a tween.\r Spring\r \r `svelte\r \r `\r \r | Option      | Description                                                          |\r | ----------- | -------------------------------------------------------------------- |\r | stiffness | Spring strength. Higher is snappier. Defaults to 100.              |\r | damping   | Opposing force. 0 never settles. Defaults to 10.                 |\r | mass      | Mass of the moving object. Defaults to 1.                          |\r | bounce    | 0–1 bounciness, as an alternative to stiffness/damping.       |\r | duration  | Approximate duration in seconds, used with bounce.                 |\r | restSpeed / restDelta | Thresholds at which the animation is considered done.  |\r | velocity  | Initial velocity.                                                    |\r Tween\r \r `svelte\r \r `\r \r ease accepts a named easing (\"linear\", \"easeIn\", \"easeOut\", \"easeInOut\", \"circIn\",\r \"backOut\", \"anticipate\", …), a cubic bezier array such as `/docs/components/motion-config sets a default transition for every descendant.\r \r `svelte\r \r  \r \r `\r \r \r \r transition={{ duration: 0 }} jumps straight to the target. To disable all animations for a\r subtree — including layout animations — use useInstantTransition or\r MotionConfig reducedMotion=\"always\".\r \r "},{"title":"motion","href":"/docs/components/motion","description":"The motion proxy drives most animations in Motion Start.","content":"\r  import { Callout, PropField, DemoContainer } from \"@svecodocs/kit\";\r  import GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r \r \r motion is a proxy: every HTML and SVG element is available on it as motion.div, motion.button,\r motion.circle, and so on. A motion component behaves exactly like its plain counterpart — same\r attributes, same events — plus animation props.\r \r `svelte\r \r  import { motion } from \"motion-start\";\r \r \r \r `\r Animation props\r \r \r The state the component mounts in. false skips the mount animation.\r \r \r \r The state to animate towards whenever it changes.\r \r \r \r The state to animate to before unmounting. Requires an AnimatePresence ancestor.\r \r \r \r How values animate. See /docs/animation/transitions.\r \r \r \r Named animation targets, so labels can be used instead of objects — and inherited by children.\r \r \r \r Like style, but transform shorthands (x, y, scale, rotate) and MotionValues are\r accepted.\r Server rendering and pre-hydration animation\r \r Motion components include their resolved initial styles in server-rendered HTML. This prevents the\r element from flashing its unanimated state before Svelte hydrates.\r \r Set appear to start eligible entrance animations as the browser parses the HTML, without waiting\r for Svelte hydration:\r \r `svelte\r \r `\r \r appear is a Motion Start extension. It applies to HTML elements whose target and transition can be\r resolved on the server, with an explicit tween or keyframes transition. Every value in animate\r must also be present in initial, since there is no computed style to read from before hydration.\r \r Springs, dynamic variant functions, transformTemplate, SVG elements, custom components,\r CSS custom properties, per-value transitions, repeats, orchestration options (when,\r delayChildren, staggerChildren) and client-side navigations all fall back to the normal\r hydration-time animation. The parser-time animation hands off to Motion during hydration without\r restarting.\r \r Omitting ease is supported: the parser-time animation uses Motion's default easeOut curve.\r MotionConfig reduced-motion settings are respected; \"user\" suppresses parser-time transform\r animations when the visitor prefers reduced motion.\r \r For a Content Security Policy that requires nonces, pass the nonce through appear:\r \r `svelte\r \r `\r Gesture props\r \r whileHover, whileTap, whileFocus, whileDrag and whileInView, plus their event handlers.\r See /docs/animation/gestures.\r Layout props\r \r layout, layoutId, layoutDependency, layoutRoot and layoutScroll. See\r /docs/animation/layout.\r Lifecycle events\r \r  void\" />\r  void\" />\r  void\" />\r Transform shorthands\r \r Transforms are independent values, so they can be animated separately and composed without fighting\r over a single transform string.\r \r `svelte\r \r `\r \r Available: x, y, z, translateX/Y/Z, scale, scaleX/Y, rotate, rotateX/Y/Z,\r skew, skewX/Y, originX/Y/Z, perspective.\r SVG\r \r SVG elements work the same way, including pathLength, pathSpacing and pathOffset, which are\r normalised to 0–1.\r \r `svelte\r \r `\r The m component\r \r m is the same component with no features bundled in. Paired with\r /docs/components/lazy-motion it keeps the initial bundle small.\r \r `svelte\r \r  import { m } from \"motion-start/m\";\r \r \r \r `\r \r \r \r Motion components forward unknown props to the underlying element, so {...rest} works as expected.\r \r "},{"title":"AnimatePresence","href":"/docs/components/animate-presence","description":"Animate elements out before Svelte removes them from the DOM.","content":"\r  import { Callout, PropField, DemoContainer } from \"@svecodocs/kit\";\r  import PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\r \r \r Svelte removes elements from the DOM as soon as an {#if} or {#each} block changes.\r AnimatePresence coordinates with Svelte's outro system so motion children can run their exit\r animation first.\r \r \r  \r \r \r `svelte\r \r  import { motion, AnimatePresence } from \"motion-start\";\r \r  let visible = $state(true);\r \r \r \r  {#if visible}\r   \r  {/if}\r \r `\r Props\r \r \r sync uses Svelte's normal outro layout. wait finishes the exit animation before the next element\r enters. popLayout removes exiting elements from the layout flow while keeping their measured\r visual position, so siblings reflow immediately.\r \r \r \r Set to false to disable the mount animation for children present on first render.\r \r \r \r Data passed to dynamic exit variants, so an element can animate out differently depending on\r context.\r \r \r \r Whether presence changes trigger layout projection in sibling layout components.\r \r \r  void\">\r Called once every exiting element in the current batch has finished animating.\r Keyed lists\r \r Inside {#each}, use a keyed block so identity is stable across updates.\r \r `svelte\r \r  {#each items as item (item.id)}\r   \r    {item.label}\r   \r  {/each}\r \r `\r Presence hooks\r \r usePresence and useIsPresent let a child read its own presence state — useful when the exit\r animation isn't driven by an exit prop.\r \r `svelte\r \r  import { usePresence } from \"motion-start\";\r \r  const [isPresent, safeToRemove] = usePresence();\r \r `\r \r usePresenceData reads the custom value from the nearest AnimatePresence.\r \r \r \r presenceAffectsLayout was a no-op before 0.1.21 and now behaves as documented. If you relied on\r the old behaviour, set it to false explicitly.\r \r "},{"title":"LayoutGroup","href":"/docs/components/layout-group","description":"Group components so they measure and animate layout together.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\r \r \r Components measure their own layout when they re-render. LayoutGroup makes a set of components\r re-measure together, so a change in one animates its siblings correctly — and it namespaces\r layoutId so shared layout animations don't collide.\r \r \r  \r \r \r `svelte\r \r  import { motion, LayoutGroup } from \"motion-start\";\r \r  let selected = $state(\"spring\");\r \r \r \r  {#each tabs as tab (tab)}\r    (selected = tab)}>\r    {#if selected === tab}\r     \r    {/if}\r    {tab}\r   \r  {/each}\r \r `\r Props\r \r | Prop      | Type                | Description                                                            |\r | --------- | ------------------- | ---------------------------------------------------------------------- |\r | id      | string            | Namespace for layoutIds within the group, so repeated groups don't share elements. |\r | inherit | boolean \\| \"id\"   | Whether to inherit the parent group's id. false isolates the group.  |\r Namespacing\r \r When the same component is rendered more than once — an accordion, a list of cards — give each\r instance its own id so their layoutIds stay distinct.\r \r `svelte\r {#each groups as group (group.id)}\r  \r   \r  \r {/each}\r `\r \r \r \r AnimateSharedLayout was removed in 0.2. Shared layout animations now work without a wrapper —\r reach for LayoutGroup when you need grouped re-measurement or namespacing.\r \r "},{"title":"LazyMotion","href":"/docs/components/lazy-motion","description":"Code-split animation features to keep the initial bundle small.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r \r \r The full motion component bundles every feature. LazyMotion lets you ship the much smaller m\r component and load only the features you need — synchronously or asynchronously.\r \r `svelte\r \r  import { LazyMotion, domAnimation } from \"motion-start\";\r  import { m } from \"motion-start/m\";\r \r \r \r  \r \r `\r Feature bundles\r \r | Bundle         | Contents                                                                |\r | -------------- | ----------------------------------------------------------------------- |\r | domMin       | Animations only.                                                        |\r | domAnimation | Animations, variants, exit animations and hover/tap/focus gestures.      |\r | domMax       | Everything, including pan, drag and layout animations.                   |\r Loading asynchronously\r \r Pass a function returning a promise to defer loading until after first paint.\r \r `svelte\r \r  import { LazyMotion } from \"motion-start\";\r  import { m } from \"motion-start/m\";\r \r  const loadFeatures = () => import(\"motion-start\").then((res) => res.domMax);\r \r \r \r  \r \r `\r Props\r \r | Prop       | Type                                  | Description                                                          |\r | ---------- | ------------------------------------- | -------------------------------------------------------------------- |\r | features | FeatureBundle \\| () => Promise    | The bundle to load, directly or lazily.                              |\r | strict   | boolean                             | Throw if a full motion component renders inside, which would undo the code-splitting. |\r \r \r \r strict makes accidental motion imports inside a LazyMotion tree a loud error rather than a\r silent bundle-size regression.\r \r "},{"title":"MotionConfig","href":"/docs/components/motion-config","description":"Set defaults for every motion component beneath it.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r \r \r MotionConfig provides configuration to all descendant motion components — most usefully a default\r transition and a reduced-motion policy.\r \r `svelte\r \r  import { MotionConfig } from \"motion-start\";\r \r \r \r  {@render children()}\r \r `\r Props\r \r | Prop            | Type                                     | Description                                                                 |\r | --------------- | ---------------------------------------- | --------------------------------------------------------------------------- |\r | transition    | Transition                             | Default transition for descendants that don't define their own.             |\r | reducedMotion | \"user\" \\| \"always\" \\| \"never\"           | How to respond to the OS reduced-motion setting. \"user\" follows the system. |\r | nonce         | string                                 | CSP nonce for injected styles.                                              |\r | isValidProp   | (key: string) => boolean               | Override which props are forwarded to the DOM element.                      |\r Reduced motion\r \r With reducedMotion=\"user\", transform and layout animations are disabled when the user has asked\r for reduced motion, while opacity and colour animations continue.\r \r `svelte\r \r  \r \r `\r \r \r \r Put MotionConfig in your root layout so the whole app shares one motion language — then override\r per component only where it matters.\r \r "},{"title":"Reorder","href":"/docs/components/reorder","description":"Drag-to-reorder lists with layout animations built in.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import ReorderDemo from \"$lib/components/demos/reorder-demo.svelte\";\r \r \r Reorder.Group and Reorder.Item turn any list into a drag-to-reorder list. Items are draggable,\r animate to their new positions, and the group reports the new order.\r \r \r  \r \r \r `svelte\r \r  import { Reorder } from \"motion-start\";\r \r  let items = $state([\"spring\", \"tween\", \"inertia\", \"keyframes\"]);\r \r \r  (items = next)}>\r  {#snippet children({ item })}\r   \r    {item}\r   \r  {/snippet}\r \r `\r Reorder.Group\r \r | Prop         | Type                        | Description                                                        |\r | ------------ | --------------------------- | ------------------------------------------------------------------ |\r | values     | T[]                       | The array being reordered. Required.                               |\r | onReorder  | (newOrder: T[]) => void   | Called with the new order as items are dragged. Required.          |\r | axis       | \"x\" \\| \"y\"                 | Drag axis. Defaults to \"y\".                                      |\r | as         | string                    | Element to render. Defaults to \"ul\".                             |\r \r The group renders its children through a snippet that receives each item, so the list stays keyed by\r value.\r Reorder.Item\r \r | Prop    | Type     | Description                                                    |\r | ------- | -------- | -------------------------------------------------------------- |\r | value | T      | The item's entry in values. Required.                        |\r | as    | string | Element to render. Defaults to \"li\".                         |\r \r Reorder.Item is a motion component, so whileDrag, transition and the other animation props all\r work.\r \r `svelte\r \r  {item}\r \r `\r \r \r \r Set dragListener={false} on the item and start the drag from a handle with useDragControls when\r the whole row shouldn't be draggable.\r \r "},{"title":"Overview","href":"/docs/motion-values/overview","description":"Track and compose animating values outside of Svelte state.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\r \r \r Motion components create motion values internally for every animating value. Creating them yourself\r lets you read velocity, chain values together, and drive animations without triggering a re-render.\r \r \r  \r \r \r `svelte\r \r  import { motion, useMotionValue, useTransform } from \"motion-start\";\r \r  const x = useMotionValue(0);\r  const rotate = useTransform(x, [-140, 140], [-24, 24]);\r \r \r \r `\r The MotionValue API\r \r | Method              | Description                                                     |\r | ------------------- | ---------------------------------------------------------------- |\r | get()             | The current value.                                              |\r | set(v)            | Set the value, without animating.                               |\r | jump(v)           | Set the value and reset velocity.                               |\r | getVelocity()     | Velocity per second.                                            |\r | on(\"change\", cb)  | Subscribe to changes. Returns an unsubscribe function.          |\r | isAnimating()     | Whether an animation is currently driving the value.            |\r | stop()            | Stop the active animation.                                      |\r \r Because updates bypass Svelte's reactivity, changing a motion value re-renders nothing — it writes\r straight to the DOM on the next frame.\r Available hooks\r \r | Hook                    | Description                                                             |\r | ----------------------- | ------------------------------------------------------------------------ |\r | useMotionValue        | Create a motion value.                                                  |\r | useTransform          | Map one or more values into a new one.                                   |\r | useSpring             | A value that springs towards its target.                                 |\r | useMotionTemplate     | Compose values into a string, e.g. a filter or background.           |\r | useScroll             | Scroll offset and progress values.                                       |\r | useTime               | Milliseconds elapsed, updated every frame.                               |\r | useVelocity           | The velocity of another motion value.                                    |\r | useMotionValueEvent   | Subscribe to a motion value event, with automatic cleanup.               |\r | useWillChange         | A will-change string that tracks the values being animated.            |\r \r \r \r Motion values are passed to a component through style. Transform shorthands like x, rotate and\r scale accept them directly, as do regular CSS properties.\r \r "},{"title":"useTransform","href":"/docs/motion-values/use-transform","description":"Map one or more motion values into a new one.","content":"\r  import { Callout, DemoContainer } from \"@svecodocs/kit\";\r  import MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\r \r \r useTransform creates a motion value derived from others. It has two forms: range mapping and a\r transformer function.\r Range mapping\r \r `ts\r const x = useMotionValue(0);\r const opacity = useTransform(x, [-200, 0, 200], [0, 1, 0]);\r `\r \r The input range must be a linear series — all increasing or all decreasing. The output range can be\r numbers, colours, shadows or any other value type Motion Start can interpolate, as long as every\r entry is the same type and format.\r \r `ts\r const background = useTransform(\r  x,\r  [-140, 0, 140],\r  [\"#e11d48\", \"#a855f7\", \"#10b981\"]\r );\r `\r \r Options are passed as a fourth argument:\r \r `ts\r const opacity = useTransform(x, [0, 100], [0, 1], { clamp: false, ease: easeOut });\r `\r Transformer functions\r \r `ts\r const time = useTime();\r const wobble = useTransform(time, (t) => Math.sin(t / 1000) * 10);\r `\r \r Multiple inputs are supported — the transformer receives an array:\r \r `ts\r const x = useMotionValue(0);\r const y = useMotionValue(0);\r const distance = useTransform([x, y], ([latestX, latestY]) =>\r  Math.hypot(latestX, latestY)\r );\r `\r \r \r \r Transformed values are themselves motion values, so they can feed useSpring, useMotionTemplate\r or another useTransform.\r \r "},{"title":"useSpring","href":"/docs/motion-values/use-spring","description":"A motion value that springs towards its target.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r \r \r useSpring creates a motion value that animates towards whatever it is set to, using spring physics.\r \r `svelte\r \r  import { motion, useSpring } from \"motion-start\";\r \r  const x = useSpring(0, { stiffness: 300, damping: 30 });\r \r \r  x.set(200)} />\r `\r Following another value\r \r Pass a motion value as the first argument and the spring will follow it — the classic way to smooth\r out a scroll or pointer value.\r \r `ts\r const { scrollYProgress } = useScroll();\r const smoothProgress = useSpring(scrollYProgress, { stiffness: 120, damping: 30 });\r `\r Options\r \r Any /docs/animation/transitions#spring is accepted: stiffness,\r damping, mass, bounce, duration, restSpeed and restDelta.\r \r `ts\r const gentle = useSpring(0, { bounce: 0.1, duration: 0.6 });\r `\r \r \r \r Use spring.jump(value) to move to a value instantly and reset velocity — useful when resetting\r state between interactions.\r \r "},{"title":"useScroll","href":"/docs/motion-values/use-scroll","description":"Motion values that track scroll offset and progress.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r \r \r useScroll returns four motion values that update as the page, a container, or a target element\r scrolls.\r \r `svelte\r \r  import { motion, useScroll } from \"motion-start\";\r \r  const { scrollYProgress } = useScroll();\r \r \r \r `\r Returned values\r \r | Value             | Description                    |\r | ----------------- | ------------------------------ |\r | scrollX         | Horizontal offset in pixels.   |\r | scrollY         | Vertical offset in pixels.     |\r | scrollXProgress | Horizontal progress, 0–1.  |\r | scrollYProgress | Vertical progress, 0–1.    |\r Options\r \r | Option         | Type                  | Description                                                            |\r | -------------- | --------------------- | ---------------------------------------------------------------------- |\r | container    | RefObject | Scrollable element to track instead of the window.                  |\r | target       | RefObject  | Element whose position within the scroll container is tracked.          |\r | offset       | ScrollOffset        | When tracking starts and ends, e.g. [\"start end\", \"end start\"].       |\r | axis         | \"x\" \\| \"y\"           | Axis for offset calculations. Defaults to \"y\".                     |\r | layoutEffect | boolean             | Set up before paint (true, the default) or after.                    |\r Tracking an element\r \r `svelte\r \r  import { motion, useScroll, useTransform } from \"motion-start\";\r \r  const ref = { current: null };\r  const { scrollYProgress } = useScroll({ target: ref, offset: [\"start end\", \"end start\"] });\r  const y = useTransform(scrollYProgress, [0, 1], [\"-20%\", \"20%\"]);\r \r \r \r  \r \r `\r \r Offsets are pairs of intersection points, where the first word refers to the target and the second\r to the container: \"start end\" means \"the start of the target meets the end of the container\".\r \r \r \r Pass a ref object created in the same component as the element. If useScroll runs before the ref\r is bound, set layoutEffect: false so setup happens after the DOM is ready.\r \r "},{"title":"useTime & useVelocity","href":"/docs/motion-values/use-time-velocity","description":"Frame-driven time and the velocity of any motion value.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r useTime\r \r useTime returns a motion value that updates every frame with the milliseconds elapsed since it was\r created. It's the simplest way to drive a perpetual animation.\r \r `svelte\r \r  import { motion, useTime, useTransform } from \"motion-start\";\r \r  const time = useTime();\r  const rotate = useTransform(time, [0, 4000], [0, 360], { clamp: false });\r \r \r \r `\r useVelocity\r \r useVelocity tracks the velocity of another motion value, in units per second.\r \r `svelte\r \r  import { motion, useScroll, useVelocity, useTransform } from \"motion-start\";\r \r  const { scrollY } = useScroll();\r  const scrollVelocity = useVelocity(scrollY);\r  const skewY = useTransform(scrollVelocity, [-1000, 1000], [-5, 5], { clamp: true });\r \r \r \r `\r \r Velocity is itself a motion value, so it can be smoothed with useSpring before use:\r \r `ts\r const smoothVelocity = useSpring(scrollVelocity, { stiffness: 400, damping: 40 });\r `\r \r \r \r useVelocity can be applied to a velocity value to get acceleration — handy for elastic,\r physical-feeling UI.\r \r "},{"title":"useMotionTemplate","href":"/docs/motion-values/use-motion-template","description":"Compose motion values into a single string value.","content":"Some CSS properties take a string built from several values — filter, background, clip-path,\r box-shadow. useMotionTemplate builds that string from motion values as a tagged template, and\r returns a new motion value that updates whenever any input changes.\r \r `svelte\r \r  import { motion, useMotionValue, useMotionTemplate } from \"motion-start\";\r \r  const blur = useMotionValue(0);\r  const brightness = useMotionValue(1);\r  const filter = useMotionTemplateblur(${blur}px) brightness(${brightness});\r \r \r \r `\r \r It composes with useTransform, so any derived value can be interpolated in:\r \r `ts\r const { scrollYProgress } = useScroll();\r const spread = useTransform(scrollYProgress, [0, 1], [0, 40]);\r const boxShadow = useMotionTemplate0 0 ${spread}px rgba(168, 85, 247, 0.6);\r `\r \r Static text and numbers can be mixed in freely — only the interpolated motion values are tracked."},{"title":"useMotionValueEvent","href":"/docs/motion-values/use-motion-value-event","description":"Subscribe to motion value events with automatic cleanup.","content":"\r  import { Callout } from \"@svecodocs/kit\";\r \r \r Motion values emit events. useMotionValueEvent subscribes to one and unsubscribes automatically\r when the component is destroyed.\r \r `svelte\r \r  import { motion, useMotionValue, useMotionValueEvent } from \"motion-start\";\r \r  const x = useMotionValue(0);\r \r  useMotionValueEvent(x, \"change\", (latest) => {\r   console.log(latest);\r  });\r \r \r \r `\r Events\r \r | Event               | Description                                          |\r | ------------------- | ---------------------------------------------------- |\r | change            | The value changed. Receives the latest value.        |\r | animationStart    | An animation started driving the value.              |\r | animationComplete | The animation finished.                              |\r | animationCancel   | The animation was interrupted.                       |\r Without the hook\r \r Outside of a component, subscribe directly and clean up yourself:\r \r `ts\r const unsubscribe = x.on(\"change\", (latest) => console.log(latest));\r `\r \r \r \r Motion value changes don't trigger Svelte reactivity. If you need a change to update the UI, copy it\r into $state inside the handler — but prefer driving the DOM with motion values where you can.\r \r "}]