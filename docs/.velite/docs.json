[
  {
    "title": "Getting Started",
    "description": "Install Motion Start and write your first animation.",
    "path": "getting-started",
    "content": "<script>\n\timport { Callout, Steps, DemoContainer } from \"@svecodocs/kit\";\n\timport PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\n</script>\n<h2>Requirements</h2>\n<p>Motion Start requires <strong>Svelte 5</strong>. Runes are used throughout the library, so a Svelte 4 project\r\nwill need to migrate first.</p>\n<h2>Install</h2>\n<pre><code class=\"language-bash\">npm install motion-start\n</code></pre>\n<p>While the rewrite is in alpha, install the <code>next</code> tag explicitly:</p>\n<pre><code class=\"language-bash\">npm install motion-start@next\n</code></pre>\n<h2>Import</h2>\n<p>Everything is exported from the package root:</p>\n<pre><code class=\"language-ts\">import { motion, AnimatePresence, useMotionValue } from \"motion-start\";\n</code></pre>\n<p>Smaller entry points are available when you only need part of the library:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Entry</th><th>Contents</th></tr></thead><tbody><tr><td><code>motion-start</code></td><td>Everything — components, hooks, values, projection.</td></tr><tr><td><code>motion-start/m</code></td><td>The <code>m</code> component only, for use with <code>LazyMotion</code>.</td></tr><tr><td><code>motion-start/dom</code></td><td>The imperative DOM API — <code>animate</code>, <code>scroll</code>, <code>inView</code>.</td></tr><tr><td><code>motion-start/mini</code></td><td>The mini animation API, backed by WAAPI.</td></tr><tr><td><code>motion-start/dom/mini</code></td><td>The mini DOM API.</td></tr><tr><td><code>motion-start/projection</code></td><td>The projection engine, for advanced layout work.</td></tr></tbody></table>\n<h2>Your first animation</h2>\n<p>Any HTML or SVG element is available on the <code>motion</code> proxy. Give it an <code>animate</code> prop and it will\r\nanimate to that state whenever the values change.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion } from \"motion-start\";\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div\r\n\tinitial={{ opacity: 0, y: 20 }}\r\n\tanimate={{ opacity: 1, y: 0 }}\r\n\ttransition={{ type: \"spring\", stiffness: 300, damping: 24 }}\r\n/>\n</code></pre>\n<ul>\n<li><code>initial</code> — the state the element mounts in.</li>\n<li><code>animate</code> — the state to animate towards.</li>\n<li><code>transition</code> — how to get there.</li>\n</ul>\n<h2>Animating elements out</h2>\n<p>Svelte removes elements from the DOM immediately, so exit animations need <code>AnimatePresence</code> to keep\r\nthem alive until the animation finishes.</p>\n<democontainer class=\"py-8\">\n\t<presencedemo>\n</presencedemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, AnimatePresence } from \"motion-start\";\r\n\r\n\tlet visible = $state(true);\r\n&#x3C;/script>\r\n\r\n&#x3C;AnimatePresence>\r\n\t{#if visible}\r\n\t\t&#x3C;motion.div\r\n\t\t\tinitial={{ opacity: 0, scale: 0.6 }}\r\n\t\t\tanimate={{ opacity: 1, scale: 1 }}\r\n\t\t\texit={{ opacity: 0, scale: 0.6 }}\r\n\t\t/>\r\n\t{/if}\r\n&#x3C;/AnimatePresence>\n</code></pre>\n<callout type=\"tip\" title=\"Keyed blocks\">\n<p>Inside <code>{#each}</code> blocks, always use a keyed block (<code>{#each items as item (item.id)}</code>) so\r\n<code>AnimatePresence</code> can tell which element is leaving.</p>\n</callout>\n<h2>Next steps</h2>\n<ul>\n<li><a href=\"/docs/animation/overview\">Animation overview</a> — variants, keyframes and orchestration.</li>\n<li><a href=\"/docs/animation/gestures\">Gestures</a> — hover, press, drag and viewport.</li>\n<li><a href=\"/docs/animation/layout\">Layout animations</a> — the <code>layout</code> prop and shared elements.</li>\n<li><a href=\"/docs/motion-values/overview\">Motion values</a> — composing values outside of Svelte state.</li>\n</ul>",
    "raw": "<script>\r\n\timport { Callout, Steps, DemoContainer } from \"@svecodocs/kit\";\r\n\timport PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\r\n</script>\r\n\r\n## Requirements\r\n\r\nMotion Start requires **Svelte 5**. Runes are used throughout the library, so a Svelte 4 project\r\nwill need to migrate first.\r\n\r\n## Install\r\n\r\n```bash\r\nnpm install motion-start\r\n```\r\n\r\nWhile the rewrite is in alpha, install the `next` tag explicitly:\r\n\r\n```bash\r\nnpm install motion-start@next\r\n```\r\n\r\n## Import\r\n\r\nEverything is exported from the package root:\r\n\r\n```ts\r\nimport { motion, AnimatePresence, useMotionValue } from \"motion-start\";\r\n```\r\n\r\nSmaller entry points are available when you only need part of the library:\r\n\r\n| Entry                    | Contents                                                    |\r\n| ------------------------ | ----------------------------------------------------------- |\r\n| `motion-start`           | Everything — components, hooks, values, projection.          |\r\n| `motion-start/m`         | The `m` component only, for use with `LazyMotion`.           |\r\n| `motion-start/dom`       | The imperative DOM API — `animate`, `scroll`, `inView`.      |\r\n| `motion-start/mini`      | The mini animation API, backed by WAAPI.                     |\r\n| `motion-start/dom/mini`  | The mini DOM API.                                            |\r\n| `motion-start/projection` | The projection engine, for advanced layout work.            |\r\n\r\n## Your first animation\r\n\r\nAny HTML or SVG element is available on the `motion` proxy. Give it an `animate` prop and it will\r\nanimate to that state whenever the values change.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion } from \"motion-start\";\r\n</script>\r\n\r\n<motion.div\r\n\tinitial={{ opacity: 0, y: 20 }}\r\n\tanimate={{ opacity: 1, y: 0 }}\r\n\ttransition={{ type: \"spring\", stiffness: 300, damping: 24 }}\r\n/>\r\n```\r\n\r\n- `initial` — the state the element mounts in.\r\n- `animate` — the state to animate towards.\r\n- `transition` — how to get there.\r\n\r\n## Animating elements out\r\n\r\nSvelte removes elements from the DOM immediately, so exit animations need `AnimatePresence` to keep\r\nthem alive until the animation finishes.\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<PresenceDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, AnimatePresence } from \"motion-start\";\r\n\r\n\tlet visible = $state(true);\r\n</script>\r\n\r\n<AnimatePresence>\r\n\t{#if visible}\r\n\t\t<motion.div\r\n\t\t\tinitial={{ opacity: 0, scale: 0.6 }}\r\n\t\t\tanimate={{ opacity: 1, scale: 1 }}\r\n\t\t\texit={{ opacity: 0, scale: 0.6 }}\r\n\t\t/>\r\n\t{/if}\r\n</AnimatePresence>\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Keyed blocks\">\r\n\r\nInside `{#each}` blocks, always use a keyed block (`{#each items as item (item.id)}`) so\r\n`AnimatePresence` can tell which element is leaving.\r\n\r\n</Callout>\r\n\r\n## Next steps\r\n\r\n- [Animation overview](/docs/animation/overview) — variants, keyframes and orchestration.\r\n- [Gestures](/docs/animation/gestures) — hover, press, drag and viewport.\r\n- [Layout animations](/docs/animation/layout) — the `layout` prop and shared elements.\r\n- [Motion values](/docs/motion-values/overview) — composing values outside of Svelte state.",
    "toc": [
      {
        "title": "Requirements",
        "url": "#requirements",
        "items": []
      },
      {
        "title": "Install",
        "url": "#install",
        "items": []
      },
      {
        "title": "Import",
        "url": "#import",
        "items": []
      },
      {
        "title": "Your first animation",
        "url": "#your-first-animation",
        "items": []
      },
      {
        "title": "Animating elements out",
        "url": "#animating-elements-out",
        "items": []
      },
      {
        "title": "Next steps",
        "url": "#next-steps",
        "items": []
      }
    ],
    "section": "Overview",
    "slug": "getting-started",
    "slugFull": "/getting-started"
  },
  {
    "title": "Introduction",
    "description": "A production-ready declarative motion library for Svelte.",
    "path": "index",
    "content": "<script>\n\timport { Callout, Card, CardGrid, DemoContainer } from \"@svecodocs/kit\";\n\timport GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\n</script>\n<p><strong>Motion Start</strong> is a declarative animation library for Svelte 5, built as a port of\r\n<a href=\"https://motion.dev\">Framer Motion</a> <code>11.11.11</code>. You describe the state you want an element to be in\r\nand Motion Start works out how to get there — including layout changes, gestures, scroll, and\r\nenter/exit animations.</p>\n<democontainer class=\"flex justify-center py-10\">\n\t<gesturedemo>\n</gesturedemo></democontainer>\n<p>Every animation above is just a prop:</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion } from \"motion-start\";\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div\r\n\twhileHover={{ scale: 1.12 }}\r\n\twhileTap={{ scale: 0.92 }}\r\n\ttransition={{ type: \"spring\", stiffness: 400, damping: 20 }}\r\n/>\n</code></pre>\n<callout type=\"warning\" title=\"Alpha\">\n<p>Motion Start is currently in alpha. APIs may still change and you should expect bugs — please\r\n<a href=\"https://github.com/JonathonRP/motion-start/issues\">report them</a> or open a PR.</p>\n</callout>\n<h2>What's new in 0.2</h2>\n<p><code>0.2.0-next.0</code> is a rewrite against <code>framer-motion@11.11.11</code> (previously <code>4.0.3</code>) on Svelte 5 runes.</p>\n<p><strong>New</strong></p>\n<ul>\n<li><code>Reorder.Group</code> / <code>Reorder.Item</code> for drag-to-reorder lists.</li>\n<li><code>LayoutGroup</code> and the full projection engine, replacing <code>AnimateSharedLayout</code>.</li>\n<li><code>AnimatePresence</code> <code>mode</code> (<code>sync</code> | <code>wait</code> | <code>popLayout</code>) and <code>usePresenceData</code>.</li>\n<li>WAAPI-accelerated animations, <code>animate()</code> sequences, and <code>stagger()</code>.</li>\n<li>Scroll (<code>useScroll</code>), viewport (<code>whileInView</code>) and <code>useInView</code> support.</li>\n<li>Motion value hooks: <code>useTime</code>, <code>useVelocity</code>, <code>useWillChange</code>, <code>useAnimate</code>, <code>useAnimateMini</code>.</li>\n<li>Subpath entries: <code>motion-start/dom</code>, <code>/mini</code>, <code>/m</code>, <code>/client</code>, <code>/projection</code>.</li>\n</ul>\n<p><strong>Changed</strong></p>\n<ul>\n<li>Components and hooks are runes-based; the renderless <code>Use*.svelte</code> wrappers are gone in favour of\r\nplain functions and classes.</li>\n<li><code>motion</code> and <code>m</code> are proxies, so any element is available as <code>motion.&#x3C;tag></code>.</li>\n<li>Gestures are rewritten onto the shared <code>Feature</code> pipeline (hover, press, pan, drag, focus).</li>\n</ul>\n<p><strong>Removed</strong></p>\n<ul>\n<li><code>AnimateSharedLayout</code> — use <code>LayoutGroup</code> with <code>layoutId</code>.</li>\n<li><code>MotionDiv</code> — use <code>motion.div</code>.</li>\n</ul>\n<h2>Explore</h2>\n<cardgrid>\n\t<card title=\"Getting Started\" href=\"/docs/getting-started\">\n\t\tInstall Motion Start and write your first animation.\n\t</card>\n\t<card title=\"Animation\" href=\"/docs/animation/overview\">\n\t\tProps, variants, keyframes and orchestration.\n\t</card>\n\t<card title=\"Gestures\" href=\"/docs/animation/gestures\">\n\t\tHover, press, pan, drag and viewport.\n\t</card>\n\t<card title=\"Layout animations\" href=\"/docs/animation/layout\">\n\t\tAnimate between any two layouts with a single prop.\n\t</card>\n\t<card title=\"Components\" href=\"/docs/components/motion\">\n\t\tmotion, AnimatePresence, LayoutGroup, Reorder and more.\n\t</card>\n\t<card title=\"Motion values\" href=\"/docs/motion-values/overview\">\n\t\tTrack, transform and compose values outside of Svelte state.\n\t</card>\n</cardgrid>",
    "raw": "<script>\r\n\timport { Callout, Card, CardGrid, DemoContainer } from \"@svecodocs/kit\";\r\n\timport GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r\n</script>\r\n\r\n**Motion Start** is a declarative animation library for Svelte 5, built as a port of\r\n[Framer Motion](https://motion.dev) `11.11.11`. You describe the state you want an element to be in\r\nand Motion Start works out how to get there — including layout changes, gestures, scroll, and\r\nenter/exit animations.\r\n\r\n<DemoContainer class=\"flex justify-center py-10\">\r\n\t<GestureDemo />\r\n</DemoContainer>\r\n\r\nEvery animation above is just a prop:\r\n\r\n```svelte\r\n<script>\r\n\timport { motion } from \"motion-start\";\r\n</script>\r\n\r\n<motion.div\r\n\twhileHover={{ scale: 1.12 }}\r\n\twhileTap={{ scale: 0.92 }}\r\n\ttransition={{ type: \"spring\", stiffness: 400, damping: 20 }}\r\n/>\r\n```\r\n\r\n<Callout type=\"warning\" title=\"Alpha\">\r\n\r\nMotion Start is currently in alpha. APIs may still change and you should expect bugs — please\r\n[report them](https://github.com/JonathonRP/motion-start/issues) or open a PR.\r\n\r\n</Callout>\r\n\r\n## What's new in 0.2\r\n\r\n`0.2.0-next.0` is a rewrite against `framer-motion@11.11.11` (previously `4.0.3`) on Svelte 5 runes.\r\n\r\n**New**\r\n\r\n- `Reorder.Group` / `Reorder.Item` for drag-to-reorder lists.\r\n- `LayoutGroup` and the full projection engine, replacing `AnimateSharedLayout`.\r\n- `AnimatePresence` `mode` (`sync` | `wait` | `popLayout`) and `usePresenceData`.\r\n- WAAPI-accelerated animations, `animate()` sequences, and `stagger()`.\r\n- Scroll (`useScroll`), viewport (`whileInView`) and `useInView` support.\r\n- Motion value hooks: `useTime`, `useVelocity`, `useWillChange`, `useAnimate`, `useAnimateMini`.\r\n- Subpath entries: `motion-start/dom`, `/mini`, `/m`, `/client`, `/projection`.\r\n\r\n**Changed**\r\n\r\n- Components and hooks are runes-based; the renderless `Use*.svelte` wrappers are gone in favour of\r\n  plain functions and classes.\r\n- `motion` and `m` are proxies, so any element is available as `motion.<tag>`.\r\n- Gestures are rewritten onto the shared `Feature` pipeline (hover, press, pan, drag, focus).\r\n\r\n**Removed**\r\n\r\n- `AnimateSharedLayout` — use `LayoutGroup` with `layoutId`.\r\n- `MotionDiv` — use `motion.div`.\r\n\r\n## Explore\r\n\r\n<CardGrid>\r\n\t<Card title=\"Getting Started\" href=\"/docs/getting-started\">\r\n\t\tInstall Motion Start and write your first animation.\r\n\t</Card>\r\n\t<Card title=\"Animation\" href=\"/docs/animation/overview\">\r\n\t\tProps, variants, keyframes and orchestration.\r\n\t</Card>\r\n\t<Card title=\"Gestures\" href=\"/docs/animation/gestures\">\r\n\t\tHover, press, pan, drag and viewport.\r\n\t</Card>\r\n\t<Card title=\"Layout animations\" href=\"/docs/animation/layout\">\r\n\t\tAnimate between any two layouts with a single prop.\r\n\t</Card>\r\n\t<Card title=\"Components\" href=\"/docs/components/motion\">\r\n\t\tmotion, AnimatePresence, LayoutGroup, Reorder and more.\r\n\t</Card>\r\n\t<Card title=\"Motion values\" href=\"/docs/motion-values/overview\">\r\n\t\tTrack, transform and compose values outside of Svelte state.\r\n\t</Card>\r\n</CardGrid>",
    "toc": [
      {
        "title": "What's new in 0.2",
        "url": "#whats-new-in-02",
        "items": []
      },
      {
        "title": "Explore",
        "url": "#explore",
        "items": []
      }
    ],
    "section": "Overview",
    "slug": "index",
    "slugFull": "/index"
  },
  {
    "title": "Overview",
    "description": "How animations are described in Motion Start.",
    "path": "animation/01_overview",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport StaggerDemo from \"$lib/components/demos/stagger-demo.svelte\";\n</script>\n<p>Motion Start scales from simple prop-based animations up to full orchestration. Animations are\r\ndeclarative: you set a target and the library animates from wherever the value currently is.</p>\n<h2>Animating props</h2>\n<p>Any value in <code>animate</code> is animated whenever it changes.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div animate={{ x: 100, backgroundColor: \"#f0f\" }} />\n</code></pre>\n<p><code>initial</code> defines the mount state. Set <code>initial={false}</code> to skip the mount animation and start at\r\nthe <code>animate</code> values.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} />\r\n&#x3C;motion.div initial={false} animate={{ opacity: 1 }} />\n</code></pre>\n<h2>Transitions</h2>\n<p><code>transition</code> controls how a value animates. Numeric and transform values default to a spring;\r\neverything else defaults to a tween.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div animate={{ x: 100 }} transition={{ type: \"spring\", stiffness: 300, damping: 20 }} />\r\n&#x3C;motion.div animate={{ opacity: 1 }} transition={{ duration: 0.4, ease: \"easeOut\" }} />\n</code></pre>\n<p>Per-value transitions are supported too:</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tanimate={{ x: 100, opacity: 1 }}\r\n\ttransition={{\r\n\t\tdefault: { type: \"spring\" },\r\n\t\topacity: { duration: 0.2, ease: \"linear\" }\r\n\t}}\r\n/>\n</code></pre>\n<p>See <a href=\"/docs/animation/transitions\">Transitions</a> for every option.</p>\n<h2>Keyframes</h2>\n<p>Pass an array to animate through a series of values. The first entry can be <code>null</code> to start from the\r\ncurrent value.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tanimate={{ x: [0, 100, 50, 0] }}\r\n\ttransition={{ duration: 1.4, times: [0, 0.3, 0.7, 1], repeat: Infinity }}\r\n/>\n</code></pre>\n<h2>Variants</h2>\n<p>Variants are named animation targets. Naming them lets a parent orchestrate its children — a child\r\nautomatically inherits the variant label of its parent unless it defines its own.</p>\n<democontainer class=\"flex justify-center py-10\">\n\t<staggerdemo>\n</staggerdemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion } from \"motion-start\";\r\n\r\n\tconst container = {\r\n\t\thidden: { opacity: 0 },\r\n\t\tvisible: {\r\n\t\t\topacity: 1,\r\n\t\t\ttransition: { staggerChildren: 0.08, delayChildren: 0.1 }\r\n\t\t}\r\n\t};\r\n\r\n\tconst item = {\r\n\t\thidden: { opacity: 0, y: 16 },\r\n\t\tvisible: { opacity: 1, y: 0 }\r\n\t};\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.ul variants={container} initial=\"hidden\" animate=\"visible\">\r\n\t{#each items as n (n)}\r\n\t\t&#x3C;motion.li variants={item} />\r\n\t{/each}\r\n&#x3C;/motion.ul>\n</code></pre>\n<p>Orchestration options available on a parent transition:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Option</th><th>Description</th></tr></thead><tbody><tr><td><code>staggerChildren</code></td><td>Delay between each child's animation, in seconds.</td></tr><tr><td><code>staggerDirection</code></td><td><code>1</code> to stagger forwards, <code>-1</code> backwards.</td></tr><tr><td><code>delayChildren</code></td><td>Delay before the first child animates.</td></tr><tr><td><code>when</code></td><td><code>\"beforeChildren\"</code> or <code>\"afterChildren\"</code>.</td></tr></tbody></table>\n<h2>Animation lifecycle</h2>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tanimate={{ x: 100 }}\r\n\tonAnimationStart={() => console.log(\"start\")}\r\n\tonAnimationComplete={(definition) => console.log(\"done\", definition)}\r\n\tonUpdate={(latest) => console.log(latest.x)}\r\n/>\n</code></pre>\n<h2>Reduced motion</h2>\n<p>Respect the user's system preference with <code>useReducedMotion</code>, or configure it globally with\r\n<a href=\"/docs/components/motion-config\"><code>MotionConfig</code></a>.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { useReducedMotion } from \"motion-start\";\r\n\r\n\tconst shouldReduceMotion = useReducedMotion();\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div animate={{ x: shouldReduceMotion() ? 0 : 100 }} />\n</code></pre>\n<callout type=\"note\" title=\"Getters, not values\">\n<p>Hooks that track reactive state — <code>useReducedMotion</code>, <code>useInView</code> — return a <em>getter</em>. Call it from\r\nreactive code (a <code>$derived</code>, a template expression) so the read stays tracked.</p>\n</callout>\n<callout type=\"note\" title=\"Imperative animations\">\n<p>For animations that aren't tied to a component's state, the DOM API is available from\r\n<code>motion-start/dom</code>: <code>animate()</code>, <code>scroll()</code>, <code>inView()</code> and <code>stagger()</code>.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport StaggerDemo from \"$lib/components/demos/stagger-demo.svelte\";\r\n</script>\r\n\r\nMotion Start scales from simple prop-based animations up to full orchestration. Animations are\r\ndeclarative: you set a target and the library animates from wherever the value currently is.\r\n\r\n## Animating props\r\n\r\nAny value in `animate` is animated whenever it changes.\r\n\r\n```svelte\r\n<motion.div animate={{ x: 100, backgroundColor: \"#f0f\" }} />\r\n```\r\n\r\n`initial` defines the mount state. Set `initial={false}` to skip the mount animation and start at\r\nthe `animate` values.\r\n\r\n```svelte\r\n<motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} />\r\n<motion.div initial={false} animate={{ opacity: 1 }} />\r\n```\r\n\r\n## Transitions\r\n\r\n`transition` controls how a value animates. Numeric and transform values default to a spring;\r\neverything else defaults to a tween.\r\n\r\n```svelte\r\n<motion.div animate={{ x: 100 }} transition={{ type: \"spring\", stiffness: 300, damping: 20 }} />\r\n<motion.div animate={{ opacity: 1 }} transition={{ duration: 0.4, ease: \"easeOut\" }} />\r\n```\r\n\r\nPer-value transitions are supported too:\r\n\r\n```svelte\r\n<motion.div\r\n\tanimate={{ x: 100, opacity: 1 }}\r\n\ttransition={{\r\n\t\tdefault: { type: \"spring\" },\r\n\t\topacity: { duration: 0.2, ease: \"linear\" }\r\n\t}}\r\n/>\r\n```\r\n\r\nSee [Transitions](/docs/animation/transitions) for every option.\r\n\r\n## Keyframes\r\n\r\nPass an array to animate through a series of values. The first entry can be `null` to start from the\r\ncurrent value.\r\n\r\n```svelte\r\n<motion.div\r\n\tanimate={{ x: [0, 100, 50, 0] }}\r\n\ttransition={{ duration: 1.4, times: [0, 0.3, 0.7, 1], repeat: Infinity }}\r\n/>\r\n```\r\n\r\n## Variants\r\n\r\nVariants are named animation targets. Naming them lets a parent orchestrate its children — a child\r\nautomatically inherits the variant label of its parent unless it defines its own.\r\n\r\n<DemoContainer class=\"flex justify-center py-10\">\r\n\t<StaggerDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<script>\r\n\timport { motion } from \"motion-start\";\r\n\r\n\tconst container = {\r\n\t\thidden: { opacity: 0 },\r\n\t\tvisible: {\r\n\t\t\topacity: 1,\r\n\t\t\ttransition: { staggerChildren: 0.08, delayChildren: 0.1 }\r\n\t\t}\r\n\t};\r\n\r\n\tconst item = {\r\n\t\thidden: { opacity: 0, y: 16 },\r\n\t\tvisible: { opacity: 1, y: 0 }\r\n\t};\r\n</script>\r\n\r\n<motion.ul variants={container} initial=\"hidden\" animate=\"visible\">\r\n\t{#each items as n (n)}\r\n\t\t<motion.li variants={item} />\r\n\t{/each}\r\n</motion.ul>\r\n```\r\n\r\nOrchestration options available on a parent transition:\r\n\r\n| Option            | Description                                                        |\r\n| ----------------- | ------------------------------------------------------------------ |\r\n| `staggerChildren` | Delay between each child's animation, in seconds.                  |\r\n| `staggerDirection` | `1` to stagger forwards, `-1` backwards.                          |\r\n| `delayChildren`   | Delay before the first child animates.                             |\r\n| `when`            | `\"beforeChildren\"` or `\"afterChildren\"`.                           |\r\n\r\n## Animation lifecycle\r\n\r\n```svelte\r\n<motion.div\r\n\tanimate={{ x: 100 }}\r\n\tonAnimationStart={() => console.log(\"start\")}\r\n\tonAnimationComplete={(definition) => console.log(\"done\", definition)}\r\n\tonUpdate={(latest) => console.log(latest.x)}\r\n/>\r\n```\r\n\r\n## Reduced motion\r\n\r\nRespect the user's system preference with `useReducedMotion`, or configure it globally with\r\n[`MotionConfig`](/docs/components/motion-config).\r\n\r\n```svelte\r\n<script>\r\n\timport { useReducedMotion } from \"motion-start\";\r\n\r\n\tconst shouldReduceMotion = useReducedMotion();\r\n</script>\r\n\r\n<motion.div animate={{ x: shouldReduceMotion() ? 0 : 100 }} />\r\n```\r\n\r\n<Callout type=\"note\" title=\"Getters, not values\">\r\n\r\nHooks that track reactive state — `useReducedMotion`, `useInView` — return a *getter*. Call it from\r\nreactive code (a `$derived`, a template expression) so the read stays tracked.\r\n\r\n</Callout>\r\n\r\n<Callout type=\"note\" title=\"Imperative animations\">\r\n\r\nFor animations that aren't tied to a component's state, the DOM API is available from\r\n`motion-start/dom`: `animate()`, `scroll()`, `inView()` and `stagger()`.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Animating props",
        "url": "#animating-props",
        "items": []
      },
      {
        "title": "Transitions",
        "url": "#transitions",
        "items": []
      },
      {
        "title": "Keyframes",
        "url": "#keyframes",
        "items": []
      },
      {
        "title": "Variants",
        "url": "#variants",
        "items": []
      },
      {
        "title": "Animation lifecycle",
        "url": "#animation-lifecycle",
        "items": []
      },
      {
        "title": "Reduced motion",
        "url": "#reduced-motion",
        "items": []
      }
    ],
    "section": "Animation",
    "slug": "animation/overview",
    "slugFull": "/animation/overview"
  },
  {
    "title": "Gestures",
    "description": "Hover, press, pan, drag and viewport detection, as animation props and event handlers.",
    "path": "animation/02_gestures",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\n\timport DragDemo from \"$lib/components/demos/drag-demo.svelte\";\n</script>\n<p>Motion Start extends Svelte's event listeners with a set of UI gestures. Each gesture has both a set\r\nof event handlers and a <code>while-</code> animation prop that applies a temporary animation target.</p>\n<democontainer class=\"flex justify-center py-10\">\n\t<gesturedemo>\n</gesturedemo></democontainer>\n<h2>Hover</h2>\n<p><code>whileHover</code> applies while a pointer is over the element. Unlike <code>:hover</code>, it only fires for actual\r\nmouse-like pointers, so it won't get stuck on touch devices.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\twhileHover={{ scale: 1.1 }}\r\n\tonHoverStart={(event, info) => {}}\r\n\tonHoverEnd={(event, info) => {}}\r\n/>\n</code></pre>\n<h2>Press</h2>\n<p><code>whileTap</code> applies from pointer-down until pointer-up. <code>onTap</code> only fires when the pointer is\r\nreleased over the same element, so dragging away cancels it.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.button\r\n\twhileTap={{ scale: 0.94 }}\r\n\tonTapStart={(event, info) => {}}\r\n\tonTap={(event, info) => {}}\r\n\tonTapCancel={(event, info) => {}}\r\n/>\n</code></pre>\n<h2>Focus</h2>\n<p><code>whileFocus</code> follows the <code>:focus-visible</code> rules — it applies on keyboard focus, not on click.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.input whileFocus={{ scale: 1.02 }} />\n</code></pre>\n<h2>Pan</h2>\n<p>Pan recognises a pointer moving across an element without moving the element itself.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tonPanStart={(event, info) => {}}\r\n\tonPan={(event, info) => console.log(info.offset.x, info.velocity.x)}\r\n\tonPanEnd={(event, info) => {}}\r\n/>\n</code></pre>\n<p><code>info</code> is a <code>PanInfo</code>: <code>point</code>, <code>delta</code>, <code>offset</code> and <code>velocity</code>.</p>\n<h2>Drag</h2>\n<democontainer class=\"py-8\">\n\t<dragdemo>\n</dragdemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tdrag\r\n\tdragConstraints={{ top: 0, left: 0, right: 240, bottom: 60 }}\r\n\tdragElastic={0.2}\r\n\twhileDrag={{ scale: 1.1, cursor: \"grabbing\" }}\r\n/>\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Description</th></tr></thead><tbody><tr><td><code>drag</code></td><td><code>true</code>, <code>\"x\"</code> or <code>\"y\"</code>.</td></tr><tr><td><code>dragConstraints</code></td><td>Pixel bounds, or a ref to an element to stay within.</td></tr><tr><td><code>dragElastic</code></td><td><code>0</code>–<code>1</code>, how far past the constraints the element can be dragged.</td></tr><tr><td><code>dragMomentum</code></td><td>Whether to continue with inertia on release. Defaults to <code>true</code>.</td></tr><tr><td><code>dragSnapToOrigin</code></td><td>Animate back to the origin on release.</td></tr><tr><td><code>dragControls</code></td><td>A <code>DragControls</code> instance for starting drags from another element.</td></tr><tr><td><code>dragListener</code></td><td>Set to <code>false</code> to only start drags via <code>dragControls</code>.</td></tr></tbody></table>\n<h3>Drag controls</h3>\n<p>Start a drag from a handle, or anywhere else:</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useDragControls } from \"motion-start\";\r\n\r\n\tconst controls = useDragControls();\r\n&#x3C;/script>\r\n\r\n&#x3C;div onpointerdown={(event) => controls.start(event, { snapToCursor: true })}>handle&#x3C;/div>\r\n\r\n&#x3C;motion.div drag dragControls={controls} dragListener={false} />\n</code></pre>\n<h2>Viewport</h2>\n<p><code>whileInView</code> animates when the element enters the viewport, and <code>viewport</code> configures detection.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tinitial={{ opacity: 0, y: 40 }}\r\n\twhileInView={{ opacity: 1, y: 0 }}\r\n\tviewport={{ once: true, amount: 0.5 }}\r\n\tonViewportEnter={(entry) => {}}\r\n\tonViewportLeave={(entry) => {}}\r\n/>\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Option</th><th>Description</th></tr></thead><tbody><tr><td><code>once</code></td><td>Only animate the first time the element enters.</td></tr><tr><td><code>amount</code></td><td><code>\"some\"</code>, <code>\"all\"</code>, or a <code>0</code>–<code>1</code> threshold.</td></tr><tr><td><code>margin</code></td><td>A root margin, e.g. <code>\"0px 0px -100px 0px\"</code>.</td></tr><tr><td><code>root</code></td><td>A ref to a scrollable ancestor to use instead of the viewport.</td></tr></tbody></table>\n<callout type=\"tip\" title=\"Gesture priority\">\n<p><code>while-</code> props are applied in order of priority: <code>whileHover</code>, <code>whileTap</code>, <code>whileDrag</code>,\r\n<code>whileFocus</code>, <code>whileInView</code>. A later gesture overrides values set by an earlier one, and the element\r\nanimates back down the stack as gestures end.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r\n\timport DragDemo from \"$lib/components/demos/drag-demo.svelte\";\r\n</script>\r\n\r\nMotion Start extends Svelte's event listeners with a set of UI gestures. Each gesture has both a set\r\nof event handlers and a `while-` animation prop that applies a temporary animation target.\r\n\r\n<DemoContainer class=\"flex justify-center py-10\">\r\n\t<GestureDemo />\r\n</DemoContainer>\r\n\r\n## Hover\r\n\r\n`whileHover` applies while a pointer is over the element. Unlike `:hover`, it only fires for actual\r\nmouse-like pointers, so it won't get stuck on touch devices.\r\n\r\n```svelte\r\n<motion.div\r\n\twhileHover={{ scale: 1.1 }}\r\n\tonHoverStart={(event, info) => {}}\r\n\tonHoverEnd={(event, info) => {}}\r\n/>\r\n```\r\n\r\n## Press\r\n\r\n`whileTap` applies from pointer-down until pointer-up. `onTap` only fires when the pointer is\r\nreleased over the same element, so dragging away cancels it.\r\n\r\n```svelte\r\n<motion.button\r\n\twhileTap={{ scale: 0.94 }}\r\n\tonTapStart={(event, info) => {}}\r\n\tonTap={(event, info) => {}}\r\n\tonTapCancel={(event, info) => {}}\r\n/>\r\n```\r\n\r\n## Focus\r\n\r\n`whileFocus` follows the `:focus-visible` rules — it applies on keyboard focus, not on click.\r\n\r\n```svelte\r\n<motion.input whileFocus={{ scale: 1.02 }} />\r\n```\r\n\r\n## Pan\r\n\r\nPan recognises a pointer moving across an element without moving the element itself.\r\n\r\n```svelte\r\n<motion.div\r\n\tonPanStart={(event, info) => {}}\r\n\tonPan={(event, info) => console.log(info.offset.x, info.velocity.x)}\r\n\tonPanEnd={(event, info) => {}}\r\n/>\r\n```\r\n\r\n`info` is a `PanInfo`: `point`, `delta`, `offset` and `velocity`.\r\n\r\n## Drag\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<DragDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<motion.div\r\n\tdrag\r\n\tdragConstraints={{ top: 0, left: 0, right: 240, bottom: 60 }}\r\n\tdragElastic={0.2}\r\n\twhileDrag={{ scale: 1.1, cursor: \"grabbing\" }}\r\n/>\r\n```\r\n\r\n| Prop              | Description                                                                 |\r\n| ----------------- | --------------------------------------------------------------------------- |\r\n| `drag`            | `true`, `\"x\"` or `\"y\"`.                                                      |\r\n| `dragConstraints` | Pixel bounds, or a ref to an element to stay within.                         |\r\n| `dragElastic`     | `0`–`1`, how far past the constraints the element can be dragged.            |\r\n| `dragMomentum`    | Whether to continue with inertia on release. Defaults to `true`.             |\r\n| `dragSnapToOrigin` | Animate back to the origin on release.                                      |\r\n| `dragControls`    | A `DragControls` instance for starting drags from another element.           |\r\n| `dragListener`    | Set to `false` to only start drags via `dragControls`.                       |\r\n\r\n### Drag controls\r\n\r\nStart a drag from a handle, or anywhere else:\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useDragControls } from \"motion-start\";\r\n\r\n\tconst controls = useDragControls();\r\n</script>\r\n\r\n<div onpointerdown={(event) => controls.start(event, { snapToCursor: true })}>handle</div>\r\n\r\n<motion.div drag dragControls={controls} dragListener={false} />\r\n```\r\n\r\n## Viewport\r\n\r\n`whileInView` animates when the element enters the viewport, and `viewport` configures detection.\r\n\r\n```svelte\r\n<motion.div\r\n\tinitial={{ opacity: 0, y: 40 }}\r\n\twhileInView={{ opacity: 1, y: 0 }}\r\n\tviewport={{ once: true, amount: 0.5 }}\r\n\tonViewportEnter={(entry) => {}}\r\n\tonViewportLeave={(entry) => {}}\r\n/>\r\n```\r\n\r\n| Option   | Description                                                            |\r\n| -------- | ---------------------------------------------------------------------- |\r\n| `once`   | Only animate the first time the element enters.                        |\r\n| `amount` | `\"some\"`, `\"all\"`, or a `0`–`1` threshold.                             |\r\n| `margin` | A root margin, e.g. `\"0px 0px -100px 0px\"`.                            |\r\n| `root`   | A ref to a scrollable ancestor to use instead of the viewport.         |\r\n\r\n<Callout type=\"tip\" title=\"Gesture priority\">\r\n\r\n`while-` props are applied in order of priority: `whileHover`, `whileTap`, `whileDrag`,\r\n`whileFocus`, `whileInView`. A later gesture overrides values set by an earlier one, and the element\r\nanimates back down the stack as gestures end.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Hover",
        "url": "#hover",
        "items": []
      },
      {
        "title": "Press",
        "url": "#press",
        "items": []
      },
      {
        "title": "Focus",
        "url": "#focus",
        "items": []
      },
      {
        "title": "Pan",
        "url": "#pan",
        "items": []
      },
      {
        "title": "Drag",
        "url": "#drag",
        "items": [
          {
            "title": "Drag controls",
            "url": "#drag-controls",
            "items": []
          }
        ]
      },
      {
        "title": "Viewport",
        "url": "#viewport",
        "items": []
      }
    ],
    "section": "Animation",
    "slug": "animation/gestures",
    "slugFull": "/animation/gestures"
  },
  {
    "title": "Layout animations",
    "description": "Animate between any two layouts, even between different elements.",
    "path": "animation/03_layout",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport LayoutDemo from \"$lib/components/demos/layout-demo.svelte\";\n\timport SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\n</script>\n<p>CSS layout properties like <code>width</code>, <code>flex</code> and <code>grid</code> are expensive to animate directly. Motion\r\nStart instead measures the element before and after the change and animates the difference using\r\ntransforms — smooth, and cheap.</p>\n<p>It's a single prop.</p>\n<democontainer class=\"py-8\">\n\t<layoutdemo>\n</layoutdemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;motion.div layout />\n</code></pre>\n<h2>Layout options</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>layout</code></td><td>Animate both position and size.</td></tr><tr><td><code>layout=\"position\"</code></td><td>Animate position only — useful for text that shouldn't stretch.</td></tr><tr><td><code>layout=\"size\"</code></td><td>Animate size only.</td></tr><tr><td><code>layoutDependency</code></td><td>Only measure when this value changes, instead of on every render.</td></tr><tr><td><code>layoutRoot</code></td><td>Make this element the projection root for its subtree.</td></tr><tr><td><code>layoutScroll</code></td><td>Mark a scrollable ancestor so scroll offsets are accounted for.</td></tr></tbody></table>\n<h2>Scale correction</h2>\n<p>Because layout animations use <code>scale</code>, children can appear distorted. Give affected children their\r\nown <code>layout</code> prop to correct them, and prefer <code>borderRadius</code> and <code>boxShadow</code> as motion values or\r\nanimated props so they're corrected automatically.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div layout>\r\n\t&#x3C;motion.span layout>label&#x3C;/motion.span>\r\n&#x3C;/motion.div>\n</code></pre>\n<h2>Shared layout animations</h2>\n<p>Give two elements the same <code>layoutId</code> and Motion Start animates between them as the first unmounts\r\nand the second mounts.</p>\n<democontainer class=\"flex justify-center py-10\">\n\t<sharedlayoutdemo>\n</sharedlayoutdemo></democontainer>\n<pre><code class=\"language-svelte\">{#each tabs as tab (tab)}\r\n\t&#x3C;button onclick={() => (selected = tab)}>\r\n\t\t{#if selected === tab}\r\n\t\t\t&#x3C;motion.span layoutId=\"tab-pill\" class=\"pill\" />\r\n\t\t{/if}\r\n\t\t{tab}\r\n\t&#x3C;/button>\r\n{/each}\n</code></pre>\n<p>Wrap the group in <a href=\"/docs/components/layout-group\"><code>LayoutGroup</code></a> when the shared elements live in\r\ndifferent components, or when sibling components need to re-measure together.</p>\n<callout type=\"warning\" title=\"Migrating from AnimateSharedLayout\">\n<p><code>AnimateSharedLayout</code> was removed in <code>0.2</code>. Use <code>LayoutGroup</code> with <code>layoutId</code> instead — shared\r\nlayout animations no longer require a wrapper unless you need grouping.</p>\n</callout>\n<h2>Layout and presence</h2>\n<p>Combine <code>layout</code> with <a href=\"/docs/components/animate-presence\"><code>AnimatePresence</code></a> so surrounding items\r\nreflow while an element animates out. Use <code>mode=\"popLayout\"</code> to take the exiting element out of the\r\nflow immediately.</p>\n<pre><code class=\"language-svelte\">&#x3C;AnimatePresence mode=\"popLayout\">\r\n\t{#each items as item (item.id)}\r\n\t\t&#x3C;motion.div layout exit={{ opacity: 0, scale: 0.8 }} />\r\n\t{/each}\r\n&#x3C;/AnimatePresence>\n</code></pre>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong>The element jumps.</strong> Its nearest scrollable ancestor probably needs <code>layoutScroll</code>.</li>\n<li><strong>Nothing animates.</strong> Layout animations need a layout change caused by a re-render; a transform\r\nchange alone won't trigger one.</li>\n<li><strong>Text stretches.</strong> Use <code>layout=\"position\"</code>.</li>\n</ul>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport LayoutDemo from \"$lib/components/demos/layout-demo.svelte\";\r\n\timport SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\r\n</script>\r\n\r\nCSS layout properties like `width`, `flex` and `grid` are expensive to animate directly. Motion\r\nStart instead measures the element before and after the change and animates the difference using\r\ntransforms — smooth, and cheap.\r\n\r\nIt's a single prop.\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<LayoutDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<motion.div layout />\r\n```\r\n\r\n## Layout options\r\n\r\n| Value              | Description                                                        |\r\n| ------------------ | ------------------------------------------------------------------ |\r\n| `layout`           | Animate both position and size.                                    |\r\n| `layout=\"position\"` | Animate position only — useful for text that shouldn't stretch.   |\r\n| `layout=\"size\"`    | Animate size only.                                                 |\r\n| `layoutDependency`  | Only measure when this value changes, instead of on every render.  |\r\n| `layoutRoot`       | Make this element the projection root for its subtree.             |\r\n| `layoutScroll`     | Mark a scrollable ancestor so scroll offsets are accounted for.    |\r\n\r\n## Scale correction\r\n\r\nBecause layout animations use `scale`, children can appear distorted. Give affected children their\r\nown `layout` prop to correct them, and prefer `borderRadius` and `boxShadow` as motion values or\r\nanimated props so they're corrected automatically.\r\n\r\n```svelte\r\n<motion.div layout>\r\n\t<motion.span layout>label</motion.span>\r\n</motion.div>\r\n```\r\n\r\n## Shared layout animations\r\n\r\nGive two elements the same `layoutId` and Motion Start animates between them as the first unmounts\r\nand the second mounts.\r\n\r\n<DemoContainer class=\"flex justify-center py-10\">\r\n\t<SharedLayoutDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n{#each tabs as tab (tab)}\r\n\t<button onclick={() => (selected = tab)}>\r\n\t\t{#if selected === tab}\r\n\t\t\t<motion.span layoutId=\"tab-pill\" class=\"pill\" />\r\n\t\t{/if}\r\n\t\t{tab}\r\n\t</button>\r\n{/each}\r\n```\r\n\r\nWrap the group in [`LayoutGroup`](/docs/components/layout-group) when the shared elements live in\r\ndifferent components, or when sibling components need to re-measure together.\r\n\r\n<Callout type=\"warning\" title=\"Migrating from AnimateSharedLayout\">\r\n\r\n`AnimateSharedLayout` was removed in `0.2`. Use `LayoutGroup` with `layoutId` instead — shared\r\nlayout animations no longer require a wrapper unless you need grouping.\r\n\r\n</Callout>\r\n\r\n## Layout and presence\r\n\r\nCombine `layout` with [`AnimatePresence`](/docs/components/animate-presence) so surrounding items\r\nreflow while an element animates out. Use `mode=\"popLayout\"` to take the exiting element out of the\r\nflow immediately.\r\n\r\n```svelte\r\n<AnimatePresence mode=\"popLayout\">\r\n\t{#each items as item (item.id)}\r\n\t\t<motion.div layout exit={{ opacity: 0, scale: 0.8 }} />\r\n\t{/each}\r\n</AnimatePresence>\r\n```\r\n\r\n## Troubleshooting\r\n\r\n- **The element jumps.** Its nearest scrollable ancestor probably needs `layoutScroll`.\r\n- **Nothing animates.** Layout animations need a layout change caused by a re-render; a transform\r\n  change alone won't trigger one.\r\n- **Text stretches.** Use `layout=\"position\"`.",
    "toc": [
      {
        "title": "Layout options",
        "url": "#layout-options",
        "items": []
      },
      {
        "title": "Scale correction",
        "url": "#scale-correction",
        "items": []
      },
      {
        "title": "Shared layout animations",
        "url": "#shared-layout-animations",
        "items": []
      },
      {
        "title": "Layout and presence",
        "url": "#layout-and-presence",
        "items": []
      },
      {
        "title": "Troubleshooting",
        "url": "#troubleshooting",
        "items": []
      }
    ],
    "section": "Animation",
    "slug": "animation/layout",
    "slugFull": "/animation/layout"
  },
  {
    "title": "Scroll",
    "description": "Scroll-linked and scroll-triggered animations.",
    "path": "animation/04_scroll",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<p>There are two kinds of scroll animation. <strong>Scroll-linked</strong> animations are tied directly to scroll\r\nposition, and <strong>scroll-triggered</strong> animations start when an element enters the viewport.</p>\n<h2>Scroll-triggered</h2>\n<p>Use <code>whileInView</code>. See <a href=\"/docs/animation/gestures#viewport\">Gestures</a> for the full <code>viewport</code> options.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.section\r\n\tinitial={{ opacity: 0, y: 40 }}\r\n\twhileInView={{ opacity: 1, y: 0 }}\r\n\tviewport={{ once: true, amount: 0.4 }}\r\n/>\n</code></pre>\n<p>For non-visual work — lazy loading, analytics — use <code>useInView</code> with an element ref.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { useInView } from \"motion-start\";\r\n\r\n\tconst ref = { current: null };\r\n\tconst isInView = useInView(ref, { once: true, amount: 0.5 });\r\n&#x3C;/script>\r\n\r\n&#x3C;section bind:this={ref.current}>\r\n\t{#if isInView()}\r\n\t\t&#x3C;Expensive />\r\n\t{/if}\r\n&#x3C;/section>\n</code></pre>\n<h2>Scroll-linked</h2>\n<p><code>useScroll</code> returns four motion values that update as the page or a container scrolls.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useScroll } from \"motion-start\";\r\n\r\n\tconst { scrollYProgress } = useScroll();\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div class=\"progress-bar\" style={{ scaleX: scrollYProgress }} />\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>scrollX</code></td><td>Horizontal scroll offset in pixels.</td></tr><tr><td><code>scrollY</code></td><td>Vertical scroll offset in pixels.</td></tr><tr><td><code>scrollXProgress</code></td><td>Horizontal progress, <code>0</code>–<code>1</code>.</td></tr><tr><td><code>scrollYProgress</code></td><td>Vertical progress, <code>0</code>–<code>1</code>.</td></tr></tbody></table>\n<h3>Tracking an element</h3>\n<p>Pass a <code>target</code> ref to track a specific element, and <code>offset</code> to define when tracking starts and\r\nends.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useScroll, useTransform } from \"motion-start\";\r\n\r\n\tconst ref = { current: null };\r\n\tconst { scrollYProgress } = useScroll({\r\n\t\ttarget: ref,\r\n\t\toffset: [\"start end\", \"end start\"]\r\n\t});\r\n\tconst opacity = useTransform(scrollYProgress, [0, 0.5, 1], [0, 1, 0]);\r\n&#x3C;/script>\r\n\r\n&#x3C;section bind:this={ref.current}>\r\n\t&#x3C;motion.div style={{ opacity }} />\r\n&#x3C;/section>\n</code></pre>\n<p><code>useScroll</code> also accepts a <code>container</code> ref to track a scrollable element instead of the window.</p>\n<callout type=\"note\" title=\"Refs\">\n<p><code>useScroll</code> expects a ref <em>object</em> (<code>{ current: element }</code>), not a bare element, so it can pick the\r\nelement up once Svelte has bound it.</p>\n</callout>\n<h2>Imperative scroll</h2>\n<p>For animations outside of components, <code>scroll()</code> from <code>motion-start/dom</code> drives any animation with\r\nscroll progress:</p>\n<pre><code class=\"language-ts\">import { animate, scroll } from \"motion-start/dom\";\r\n\r\nscroll(animate(\"#header\", { backgroundColor: [\"#fff\", \"#000\"] }, { ease: \"linear\" }));\n</code></pre>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\nThere are two kinds of scroll animation. **Scroll-linked** animations are tied directly to scroll\r\nposition, and **scroll-triggered** animations start when an element enters the viewport.\r\n\r\n## Scroll-triggered\r\n\r\nUse `whileInView`. See [Gestures](/docs/animation/gestures#viewport) for the full `viewport` options.\r\n\r\n```svelte\r\n<motion.section\r\n\tinitial={{ opacity: 0, y: 40 }}\r\n\twhileInView={{ opacity: 1, y: 0 }}\r\n\tviewport={{ once: true, amount: 0.4 }}\r\n/>\r\n```\r\n\r\nFor non-visual work — lazy loading, analytics — use `useInView` with an element ref.\r\n\r\n```svelte\r\n<script>\r\n\timport { useInView } from \"motion-start\";\r\n\r\n\tconst ref = { current: null };\r\n\tconst isInView = useInView(ref, { once: true, amount: 0.5 });\r\n</script>\r\n\r\n<section bind:this={ref.current}>\r\n\t{#if isInView()}\r\n\t\t<Expensive />\r\n\t{/if}\r\n</section>\r\n```\r\n\r\n## Scroll-linked\r\n\r\n`useScroll` returns four motion values that update as the page or a container scrolls.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useScroll } from \"motion-start\";\r\n\r\n\tconst { scrollYProgress } = useScroll();\r\n</script>\r\n\r\n<motion.div class=\"progress-bar\" style={{ scaleX: scrollYProgress }} />\r\n```\r\n\r\n| Value             | Description                                        |\r\n| ----------------- | -------------------------------------------------- |\r\n| `scrollX`         | Horizontal scroll offset in pixels.                |\r\n| `scrollY`         | Vertical scroll offset in pixels.                  |\r\n| `scrollXProgress` | Horizontal progress, `0`–`1`.                      |\r\n| `scrollYProgress` | Vertical progress, `0`–`1`.                        |\r\n\r\n### Tracking an element\r\n\r\nPass a `target` ref to track a specific element, and `offset` to define when tracking starts and\r\nends.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useScroll, useTransform } from \"motion-start\";\r\n\r\n\tconst ref = { current: null };\r\n\tconst { scrollYProgress } = useScroll({\r\n\t\ttarget: ref,\r\n\t\toffset: [\"start end\", \"end start\"]\r\n\t});\r\n\tconst opacity = useTransform(scrollYProgress, [0, 0.5, 1], [0, 1, 0]);\r\n</script>\r\n\r\n<section bind:this={ref.current}>\r\n\t<motion.div style={{ opacity }} />\r\n</section>\r\n```\r\n\r\n`useScroll` also accepts a `container` ref to track a scrollable element instead of the window.\r\n\r\n<Callout type=\"note\" title=\"Refs\">\r\n\r\n`useScroll` expects a ref *object* (`{ current: element }`), not a bare element, so it can pick the\r\nelement up once Svelte has bound it.\r\n\r\n</Callout>\r\n\r\n## Imperative scroll\r\n\r\nFor animations outside of components, `scroll()` from `motion-start/dom` drives any animation with\r\nscroll progress:\r\n\r\n```ts\r\nimport { animate, scroll } from \"motion-start/dom\";\r\n\r\nscroll(animate(\"#header\", { backgroundColor: [\"#fff\", \"#000\"] }, { ease: \"linear\" }));\r\n```",
    "toc": [
      {
        "title": "Scroll-triggered",
        "url": "#scroll-triggered",
        "items": []
      },
      {
        "title": "Scroll-linked",
        "url": "#scroll-linked",
        "items": [
          {
            "title": "Tracking an element",
            "url": "#tracking-an-element",
            "items": []
          }
        ]
      },
      {
        "title": "Imperative scroll",
        "url": "#imperative-scroll",
        "items": []
      }
    ],
    "section": "Animation",
    "slug": "animation/scroll",
    "slugFull": "/animation/scroll"
  },
  {
    "title": "Transitions",
    "description": "Springs, tweens, inertia and orchestration options.",
    "path": "animation/05_transitions",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport TransitionDemo from \"$lib/components/demos/transition-demo.svelte\";\n</script>\n<p>A <code>transition</code> describes <em>how</em> a value animates. Transforms and other numeric values default to a\r\nspring; colours, opacity and non-physical values default to a tween.</p>\n<democontainer class=\"py-8\">\n\t<transitiondemo>\n</transitiondemo></democontainer>\n<h2>Spring</h2>\n<pre><code class=\"language-svelte\">&#x3C;motion.div animate={{ x: 100 }} transition={{ type: \"spring\", stiffness: 300, damping: 20 }} />\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Option</th><th>Description</th></tr></thead><tbody><tr><td><code>stiffness</code></td><td>Spring strength. Higher is snappier. Defaults to <code>100</code>.</td></tr><tr><td><code>damping</code></td><td>Opposing force. <code>0</code> never settles. Defaults to <code>10</code>.</td></tr><tr><td><code>mass</code></td><td>Mass of the moving object. Defaults to <code>1</code>.</td></tr><tr><td><code>bounce</code></td><td><code>0</code>–<code>1</code> bounciness, as an alternative to <code>stiffness</code>/<code>damping</code>.</td></tr><tr><td><code>duration</code></td><td>Approximate duration in seconds, used with <code>bounce</code>.</td></tr><tr><td><code>restSpeed</code> / <code>restDelta</code></td><td>Thresholds at which the animation is considered done.</td></tr><tr><td><code>velocity</code></td><td>Initial velocity.</td></tr></tbody></table>\n<h2>Tween</h2>\n<pre><code class=\"language-svelte\">&#x3C;motion.div animate={{ opacity: 1 }} transition={{ duration: 0.4, ease: \"easeInOut\" }} />\n</code></pre>\n<p><code>ease</code> accepts a named easing (<code>\"linear\"</code>, <code>\"easeIn\"</code>, <code>\"easeOut\"</code>, <code>\"easeInOut\"</code>, <code>\"circIn\"</code>,\r\n<code>\"backOut\"</code>, <code>\"anticipate\"</code>, …), a cubic bezier array such as <code>[0.17, 0.67, 0.83, 0.67]</code>, or a\r\ncustom function.</p>\n<p>Other tween options: <code>delay</code>, <code>repeat</code>, <code>repeatType</code> (<code>\"loop\"</code> | <code>\"reverse\"</code> | <code>\"mirror\"</code>),\r\n<code>repeatDelay</code>, and <code>times</code> for keyframe positions.</p>\n<h2>Inertia</h2>\n<p>Inertia decelerates a value from its current velocity, optionally snapping into bounds. This is what\r\n<code>drag</code> uses on release.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tdrag\r\n\tdragTransition={{ power: 0.3, timeConstant: 200, bounceStiffness: 500, bounceDamping: 40 }}\r\n/>\n</code></pre>\n<h2>Per-value transitions</h2>\n<pre><code class=\"language-svelte\">&#x3C;motion.div\r\n\tanimate={{ x: 100, opacity: 1 }}\r\n\ttransition={{\r\n\t\tdefault: { type: \"spring\", stiffness: 260 },\r\n\t\topacity: { duration: 0.2, ease: \"linear\" }\r\n\t}}\r\n/>\n</code></pre>\n<h2>Orchestration</h2>\n<p>Applied to a parent with <code>variants</code>:</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.ul\r\n\tvariants={{\r\n\t\tvisible: {\r\n\t\t\topacity: 1,\r\n\t\t\ttransition: { when: \"beforeChildren\", staggerChildren: 0.08, delayChildren: 0.2 }\r\n\t\t}\r\n\t}}\r\n/>\n</code></pre>\n<h2>Global defaults</h2>\n<p><a href=\"/docs/components/motion-config\"><code>MotionConfig</code></a> sets a default transition for every descendant.</p>\n<pre><code class=\"language-svelte\">&#x3C;MotionConfig transition={{ type: \"spring\", stiffness: 240, damping: 24 }}>\r\n\t&#x3C;App />\r\n&#x3C;/MotionConfig>\n</code></pre>\n<callout type=\"tip\" title=\"Instant transitions\">\n<p><code>transition={{ duration: 0 }}</code> jumps straight to the target. To disable <em>all</em> animations for a\r\nsubtree — including layout animations — use <code>useInstantTransition</code> or\r\n<code>MotionConfig reducedMotion=\"always\"</code>.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport TransitionDemo from \"$lib/components/demos/transition-demo.svelte\";\r\n</script>\r\n\r\nA `transition` describes *how* a value animates. Transforms and other numeric values default to a\r\nspring; colours, opacity and non-physical values default to a tween.\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<TransitionDemo />\r\n</DemoContainer>\r\n\r\n## Spring\r\n\r\n```svelte\r\n<motion.div animate={{ x: 100 }} transition={{ type: \"spring\", stiffness: 300, damping: 20 }} />\r\n```\r\n\r\n| Option      | Description                                                          |\r\n| ----------- | -------------------------------------------------------------------- |\r\n| `stiffness` | Spring strength. Higher is snappier. Defaults to `100`.              |\r\n| `damping`   | Opposing force. `0` never settles. Defaults to `10`.                 |\r\n| `mass`      | Mass of the moving object. Defaults to `1`.                          |\r\n| `bounce`    | `0`–`1` bounciness, as an alternative to `stiffness`/`damping`.       |\r\n| `duration`  | Approximate duration in seconds, used with `bounce`.                 |\r\n| `restSpeed` / `restDelta` | Thresholds at which the animation is considered done.  |\r\n| `velocity`  | Initial velocity.                                                    |\r\n\r\n## Tween\r\n\r\n```svelte\r\n<motion.div animate={{ opacity: 1 }} transition={{ duration: 0.4, ease: \"easeInOut\" }} />\r\n```\r\n\r\n`ease` accepts a named easing (`\"linear\"`, `\"easeIn\"`, `\"easeOut\"`, `\"easeInOut\"`, `\"circIn\"`,\r\n`\"backOut\"`, `\"anticipate\"`, …), a cubic bezier array such as `[0.17, 0.67, 0.83, 0.67]`, or a\r\ncustom function.\r\n\r\nOther tween options: `delay`, `repeat`, `repeatType` (`\"loop\"` | `\"reverse\"` | `\"mirror\"`),\r\n`repeatDelay`, and `times` for keyframe positions.\r\n\r\n## Inertia\r\n\r\nInertia decelerates a value from its current velocity, optionally snapping into bounds. This is what\r\n`drag` uses on release.\r\n\r\n```svelte\r\n<motion.div\r\n\tdrag\r\n\tdragTransition={{ power: 0.3, timeConstant: 200, bounceStiffness: 500, bounceDamping: 40 }}\r\n/>\r\n```\r\n\r\n## Per-value transitions\r\n\r\n```svelte\r\n<motion.div\r\n\tanimate={{ x: 100, opacity: 1 }}\r\n\ttransition={{\r\n\t\tdefault: { type: \"spring\", stiffness: 260 },\r\n\t\topacity: { duration: 0.2, ease: \"linear\" }\r\n\t}}\r\n/>\r\n```\r\n\r\n## Orchestration\r\n\r\nApplied to a parent with `variants`:\r\n\r\n```svelte\r\n<motion.ul\r\n\tvariants={{\r\n\t\tvisible: {\r\n\t\t\topacity: 1,\r\n\t\t\ttransition: { when: \"beforeChildren\", staggerChildren: 0.08, delayChildren: 0.2 }\r\n\t\t}\r\n\t}}\r\n/>\r\n```\r\n\r\n## Global defaults\r\n\r\n[`MotionConfig`](/docs/components/motion-config) sets a default transition for every descendant.\r\n\r\n```svelte\r\n<MotionConfig transition={{ type: \"spring\", stiffness: 240, damping: 24 }}>\r\n\t<App />\r\n</MotionConfig>\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Instant transitions\">\r\n\r\n`transition={{ duration: 0 }}` jumps straight to the target. To disable *all* animations for a\r\nsubtree — including layout animations — use `useInstantTransition` or\r\n`MotionConfig reducedMotion=\"always\"`.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Spring",
        "url": "#spring",
        "items": []
      },
      {
        "title": "Tween",
        "url": "#tween",
        "items": []
      },
      {
        "title": "Inertia",
        "url": "#inertia",
        "items": []
      },
      {
        "title": "Per-value transitions",
        "url": "#per-value-transitions",
        "items": []
      },
      {
        "title": "Orchestration",
        "url": "#orchestration",
        "items": []
      },
      {
        "title": "Global defaults",
        "url": "#global-defaults",
        "items": []
      }
    ],
    "section": "Animation",
    "slug": "animation/transitions",
    "slugFull": "/animation/transitions"
  },
  {
    "title": "motion",
    "description": "The motion proxy drives most animations in Motion Start.",
    "path": "components/01_motion",
    "content": "<script>\n\timport { Callout, PropField, DemoContainer } from \"@svecodocs/kit\";\n\timport GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\n</script>\n<p><code>motion</code> is a proxy: every HTML and SVG element is available on it as <code>motion.div</code>, <code>motion.button</code>,\r\n<code>motion.circle</code>, and so on. A motion component behaves exactly like its plain counterpart — same\r\nattributes, same events — plus animation props.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion } from \"motion-start\";\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div\r\n\tinitial={{ opacity: 0 }}\r\n\tanimate={{ opacity: 1 }}\r\n\texit={{ opacity: 0 }}\r\n\twhileHover={{ scale: 1.05 }}\r\n\ttransition={{ type: \"spring\" }}\r\n/>\n</code></pre>\n<democontainer class=\"flex justify-center py-10\">\n\t<gesturedemo>\n</gesturedemo></democontainer>\n<h2>Animation props</h2>\n<propfield name=\"initial\" type=\"Target | VariantLabels | false\">\nThe state the component mounts in. `false` skips the mount animation.\n</propfield>\n<propfield name=\"animate\" type=\"Target | VariantLabels | AnimationControls\">\nThe state to animate towards whenever it changes.\n</propfield>\n<propfield name=\"exit\" type=\"Target | VariantLabels\">\nThe state to animate to before unmounting. Requires an `AnimatePresence` ancestor.\n</propfield>\n<propfield name=\"transition\" type=\"Transition\">\nHow values animate. See [Transitions](/docs/animation/transitions).\n</propfield>\n<propfield name=\"variants\" type=\"Variants\">\nNamed animation targets, so labels can be used instead of objects — and inherited by children.\n</propfield>\n<propfield name=\"style\" type=\"MotionStyle\">\nLike `style`, but transform shorthands (`x`, `y`, `scale`, `rotate`) and `MotionValue`s are\naccepted.\n</propfield>\n<h2>Gesture props</h2>\n<p><code>whileHover</code>, <code>whileTap</code>, <code>whileFocus</code>, <code>whileDrag</code> and <code>whileInView</code>, plus their event handlers.\r\nSee <a href=\"/docs/animation/gestures\">Gestures</a>.</p>\n<h2>Layout props</h2>\n<p><code>layout</code>, <code>layoutId</code>, <code>layoutDependency</code>, <code>layoutRoot</code> and <code>layoutScroll</code>. See\r\n<a href=\"/docs/animation/layout\">Layout animations</a>.</p>\n<h2>Lifecycle events</h2>\n<propfield name=\"onAnimationStart\" type=\"(definition) => void\">\n<propfield name=\"onAnimationComplete\" type=\"(definition) => void\">\n<propfield name=\"onUpdate\" type=\"(latest: ResolvedValues) => void\">\n<h2>Transform shorthands</h2>\n<p>Transforms are independent values, so they can be animated separately and composed without fighting\r\nover a single <code>transform</code> string.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.div style={{ x: 100, rotate: 45, scale: 1.2 }} />\n</code></pre>\n<p>Available: <code>x</code>, <code>y</code>, <code>z</code>, <code>translateX/Y/Z</code>, <code>scale</code>, <code>scaleX/Y</code>, <code>rotate</code>, <code>rotateX/Y/Z</code>,\r\n<code>skew</code>, <code>skewX/Y</code>, <code>originX/Y/Z</code>, <code>perspective</code>.</p>\n<h2>SVG</h2>\n<p>SVG elements work the same way, including <code>pathLength</code>, <code>pathSpacing</code> and <code>pathOffset</code>, which are\r\nnormalised to <code>0</code>–<code>1</code>.</p>\n<pre><code class=\"language-svelte\">&#x3C;motion.path initial={{ pathLength: 0 }} animate={{ pathLength: 1 }} />\n</code></pre>\n<h2>The <code>m</code> component</h2>\n<p><code>m</code> is the same component with no features bundled in. Paired with\r\n<a href=\"/docs/components/lazy-motion\"><code>LazyMotion</code></a> it keeps the initial bundle small.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { m } from \"motion-start/m\";\r\n&#x3C;/script>\r\n\r\n&#x3C;m.div animate={{ opacity: 1 }} />\n</code></pre>\n<callout type=\"note\" title=\"Spreading props\">\n<p>Motion components forward unknown props to the underlying element, so <code>{...rest}</code> works as expected.</p>\n</callout></propfield></propfield></propfield>",
    "raw": "<script>\r\n\timport { Callout, PropField, DemoContainer } from \"@svecodocs/kit\";\r\n\timport GestureDemo from \"$lib/components/demos/gesture-demo.svelte\";\r\n</script>\r\n\r\n`motion` is a proxy: every HTML and SVG element is available on it as `motion.div`, `motion.button`,\r\n`motion.circle`, and so on. A motion component behaves exactly like its plain counterpart — same\r\nattributes, same events — plus animation props.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion } from \"motion-start\";\r\n</script>\r\n\r\n<motion.div\r\n\tinitial={{ opacity: 0 }}\r\n\tanimate={{ opacity: 1 }}\r\n\texit={{ opacity: 0 }}\r\n\twhileHover={{ scale: 1.05 }}\r\n\ttransition={{ type: \"spring\" }}\r\n/>\r\n```\r\n\r\n<DemoContainer class=\"flex justify-center py-10\">\r\n\t<GestureDemo />\r\n</DemoContainer>\r\n\r\n## Animation props\r\n\r\n<PropField name=\"initial\" type=\"Target | VariantLabels | false\">\r\nThe state the component mounts in. `false` skips the mount animation.\r\n</PropField>\r\n\r\n<PropField name=\"animate\" type=\"Target | VariantLabels | AnimationControls\">\r\nThe state to animate towards whenever it changes.\r\n</PropField>\r\n\r\n<PropField name=\"exit\" type=\"Target | VariantLabels\">\r\nThe state to animate to before unmounting. Requires an `AnimatePresence` ancestor.\r\n</PropField>\r\n\r\n<PropField name=\"transition\" type=\"Transition\">\r\nHow values animate. See [Transitions](/docs/animation/transitions).\r\n</PropField>\r\n\r\n<PropField name=\"variants\" type=\"Variants\">\r\nNamed animation targets, so labels can be used instead of objects — and inherited by children.\r\n</PropField>\r\n\r\n<PropField name=\"style\" type=\"MotionStyle\">\r\nLike `style`, but transform shorthands (`x`, `y`, `scale`, `rotate`) and `MotionValue`s are\r\naccepted.\r\n</PropField>\r\n\r\n## Gesture props\r\n\r\n`whileHover`, `whileTap`, `whileFocus`, `whileDrag` and `whileInView`, plus their event handlers.\r\nSee [Gestures](/docs/animation/gestures).\r\n\r\n## Layout props\r\n\r\n`layout`, `layoutId`, `layoutDependency`, `layoutRoot` and `layoutScroll`. See\r\n[Layout animations](/docs/animation/layout).\r\n\r\n## Lifecycle events\r\n\r\n<PropField name=\"onAnimationStart\" type=\"(definition) => void\" />\r\n<PropField name=\"onAnimationComplete\" type=\"(definition) => void\" />\r\n<PropField name=\"onUpdate\" type=\"(latest: ResolvedValues) => void\" />\r\n\r\n## Transform shorthands\r\n\r\nTransforms are independent values, so they can be animated separately and composed without fighting\r\nover a single `transform` string.\r\n\r\n```svelte\r\n<motion.div style={{ x: 100, rotate: 45, scale: 1.2 }} />\r\n```\r\n\r\nAvailable: `x`, `y`, `z`, `translateX/Y/Z`, `scale`, `scaleX/Y`, `rotate`, `rotateX/Y/Z`,\r\n`skew`, `skewX/Y`, `originX/Y/Z`, `perspective`.\r\n\r\n## SVG\r\n\r\nSVG elements work the same way, including `pathLength`, `pathSpacing` and `pathOffset`, which are\r\nnormalised to `0`–`1`.\r\n\r\n```svelte\r\n<motion.path initial={{ pathLength: 0 }} animate={{ pathLength: 1 }} />\r\n```\r\n\r\n## The `m` component\r\n\r\n`m` is the same component with no features bundled in. Paired with\r\n[`LazyMotion`](/docs/components/lazy-motion) it keeps the initial bundle small.\r\n\r\n```svelte\r\n<script>\r\n\timport { m } from \"motion-start/m\";\r\n</script>\r\n\r\n<m.div animate={{ opacity: 1 }} />\r\n```\r\n\r\n<Callout type=\"note\" title=\"Spreading props\">\r\n\r\nMotion components forward unknown props to the underlying element, so `{...rest}` works as expected.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Animation props",
        "url": "#animation-props",
        "items": []
      },
      {
        "title": "Gesture props",
        "url": "#gesture-props",
        "items": []
      },
      {
        "title": "Layout props",
        "url": "#layout-props",
        "items": []
      },
      {
        "title": "Lifecycle events",
        "url": "#lifecycle-events",
        "items": []
      },
      {
        "title": "Transform shorthands",
        "url": "#transform-shorthands",
        "items": []
      },
      {
        "title": "SVG",
        "url": "#svg",
        "items": []
      },
      {
        "title": "The m component",
        "url": "#the-m-component",
        "items": []
      }
    ],
    "section": "Components",
    "slug": "components/motion",
    "slugFull": "/components/motion"
  },
  {
    "title": "AnimatePresence",
    "description": "Animate elements out before Svelte removes them from the DOM.",
    "path": "components/02_animate-presence",
    "content": "<script>\n\timport { Callout, PropField, DemoContainer } from \"@svecodocs/kit\";\n\timport PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\n</script>\n<p>Svelte removes elements from the DOM as soon as an <code>{#if}</code> or <code>{#each}</code> block changes.\r\n<code>AnimatePresence</code> coordinates with Svelte's outro system so motion children can run their <code>exit</code>\r\nanimation first.</p>\n<democontainer class=\"py-8\">\n\t<presencedemo>\n</presencedemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, AnimatePresence } from \"motion-start\";\r\n\r\n\tlet visible = $state(true);\r\n&#x3C;/script>\r\n\r\n&#x3C;AnimatePresence>\r\n\t{#if visible}\r\n\t\t&#x3C;motion.div\r\n\t\t\tinitial={{ opacity: 0, scale: 0.6 }}\r\n\t\t\tanimate={{ opacity: 1, scale: 1 }}\r\n\t\t\texit={{ opacity: 0, scale: 0.6 }}\r\n\t\t/>\r\n\t{/if}\r\n&#x3C;/AnimatePresence>\n</code></pre>\n<h2>Props</h2>\n<propfield name=\"mode\" type=\"&#x22;sync&#x22; | &#x22;wait&#x22; | &#x22;popLayout&#x22;\" defaultvalue=\"&#x22;sync&#x22;\">\n`sync` uses Svelte's normal outro layout. `wait` finishes the exit animation before the next element\nenters. `popLayout` removes exiting elements from the layout flow while keeping their measured\nvisual position, so siblings reflow immediately.\n</propfield>\n<propfield name=\"initial\" type=\"boolean\" defaultvalue=\"true\">\nSet to `false` to disable the mount animation for children present on first render.\n</propfield>\n<propfield name=\"custom\" type=\"unknown\">\nData passed to dynamic `exit` variants, so an element can animate out differently depending on\ncontext.\n</propfield>\n<propfield name=\"presenceAffectsLayout\" type=\"boolean\" defaultvalue=\"true\">\nWhether presence changes trigger layout projection in sibling `layout` components.\n</propfield>\n<propfield name=\"onExitComplete\" type=\"() => void\">\nCalled once every exiting element in the current batch has finished animating.\n</propfield>\n<h2>Keyed lists</h2>\n<p>Inside <code>{#each}</code>, use a keyed block so identity is stable across updates.</p>\n<pre><code class=\"language-svelte\">&#x3C;AnimatePresence mode=\"popLayout\">\r\n\t{#each items as item (item.id)}\r\n\t\t&#x3C;motion.li\r\n\t\t\tlayout\r\n\t\t\tinitial={{ opacity: 0, y: -20 }}\r\n\t\t\tanimate={{ opacity: 1, y: 0 }}\r\n\t\t\texit={{ opacity: 0, y: 20 }}\r\n\t\t>\r\n\t\t\t{item.label}\r\n\t\t&#x3C;/motion.li>\r\n\t{/each}\r\n&#x3C;/AnimatePresence>\n</code></pre>\n<h2>Presence hooks</h2>\n<p><code>usePresence</code> and <code>useIsPresent</code> let a child read its own presence state — useful when the exit\r\nanimation isn't driven by an <code>exit</code> prop.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { usePresence } from \"motion-start\";\r\n\r\n\tconst [isPresent, safeToRemove] = usePresence();\r\n&#x3C;/script>\n</code></pre>\n<p><code>usePresenceData</code> reads the <code>custom</code> value from the nearest <code>AnimatePresence</code>.</p>\n<callout type=\"warning\" title=\"Migrating\">\n<p><code>presenceAffectsLayout</code> was a no-op before <code>0.1.21</code> and now behaves as documented. If you relied on\r\nthe old behaviour, set it to <code>false</code> explicitly.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, PropField, DemoContainer } from \"@svecodocs/kit\";\r\n\timport PresenceDemo from \"$lib/components/demos/presence-demo.svelte\";\r\n</script>\r\n\r\nSvelte removes elements from the DOM as soon as an `{#if}` or `{#each}` block changes.\r\n`AnimatePresence` coordinates with Svelte's outro system so motion children can run their `exit`\r\nanimation first.\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<PresenceDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, AnimatePresence } from \"motion-start\";\r\n\r\n\tlet visible = $state(true);\r\n</script>\r\n\r\n<AnimatePresence>\r\n\t{#if visible}\r\n\t\t<motion.div\r\n\t\t\tinitial={{ opacity: 0, scale: 0.6 }}\r\n\t\t\tanimate={{ opacity: 1, scale: 1 }}\r\n\t\t\texit={{ opacity: 0, scale: 0.6 }}\r\n\t\t/>\r\n\t{/if}\r\n</AnimatePresence>\r\n```\r\n\r\n## Props\r\n\r\n<PropField name=\"mode\" type='\"sync\" | \"wait\" | \"popLayout\"' defaultValue='\"sync\"'>\r\n`sync` uses Svelte's normal outro layout. `wait` finishes the exit animation before the next element\r\nenters. `popLayout` removes exiting elements from the layout flow while keeping their measured\r\nvisual position, so siblings reflow immediately.\r\n</PropField>\r\n\r\n<PropField name=\"initial\" type=\"boolean\" defaultValue=\"true\">\r\nSet to `false` to disable the mount animation for children present on first render.\r\n</PropField>\r\n\r\n<PropField name=\"custom\" type=\"unknown\">\r\nData passed to dynamic `exit` variants, so an element can animate out differently depending on\r\ncontext.\r\n</PropField>\r\n\r\n<PropField name=\"presenceAffectsLayout\" type=\"boolean\" defaultValue=\"true\">\r\nWhether presence changes trigger layout projection in sibling `layout` components.\r\n</PropField>\r\n\r\n<PropField name=\"onExitComplete\" type=\"() => void\">\r\nCalled once every exiting element in the current batch has finished animating.\r\n</PropField>\r\n\r\n## Keyed lists\r\n\r\nInside `{#each}`, use a keyed block so identity is stable across updates.\r\n\r\n```svelte\r\n<AnimatePresence mode=\"popLayout\">\r\n\t{#each items as item (item.id)}\r\n\t\t<motion.li\r\n\t\t\tlayout\r\n\t\t\tinitial={{ opacity: 0, y: -20 }}\r\n\t\t\tanimate={{ opacity: 1, y: 0 }}\r\n\t\t\texit={{ opacity: 0, y: 20 }}\r\n\t\t>\r\n\t\t\t{item.label}\r\n\t\t</motion.li>\r\n\t{/each}\r\n</AnimatePresence>\r\n```\r\n\r\n## Presence hooks\r\n\r\n`usePresence` and `useIsPresent` let a child read its own presence state — useful when the exit\r\nanimation isn't driven by an `exit` prop.\r\n\r\n```svelte\r\n<script>\r\n\timport { usePresence } from \"motion-start\";\r\n\r\n\tconst [isPresent, safeToRemove] = usePresence();\r\n</script>\r\n```\r\n\r\n`usePresenceData` reads the `custom` value from the nearest `AnimatePresence`.\r\n\r\n<Callout type=\"warning\" title=\"Migrating\">\r\n\r\n`presenceAffectsLayout` was a no-op before `0.1.21` and now behaves as documented. If you relied on\r\nthe old behaviour, set it to `false` explicitly.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Props",
        "url": "#props",
        "items": []
      },
      {
        "title": "Keyed lists",
        "url": "#keyed-lists",
        "items": []
      },
      {
        "title": "Presence hooks",
        "url": "#presence-hooks",
        "items": []
      }
    ],
    "section": "Components",
    "slug": "components/animate-presence",
    "slugFull": "/components/animate-presence"
  },
  {
    "title": "LayoutGroup",
    "description": "Group components so they measure and animate layout together.",
    "path": "components/03_layout-group",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\n</script>\n<p>Components measure their own layout when they re-render. <code>LayoutGroup</code> makes a set of components\r\nre-measure together, so a change in one animates its siblings correctly — and it namespaces\r\n<code>layoutId</code> so shared layout animations don't collide.</p>\n<democontainer class=\"flex justify-center py-10\">\n\t<sharedlayoutdemo>\n</sharedlayoutdemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, LayoutGroup } from \"motion-start\";\r\n\r\n\tlet selected = $state(\"spring\");\r\n&#x3C;/script>\r\n\r\n&#x3C;LayoutGroup>\r\n\t{#each tabs as tab (tab)}\r\n\t\t&#x3C;button onclick={() => (selected = tab)}>\r\n\t\t\t{#if selected === tab}\r\n\t\t\t\t&#x3C;motion.span layoutId=\"pill\" />\r\n\t\t\t{/if}\r\n\t\t\t{tab}\r\n\t\t&#x3C;/button>\r\n\t{/each}\r\n&#x3C;/LayoutGroup>\n</code></pre>\n<h2>Props</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td>Namespace for <code>layoutId</code>s within the group, so repeated groups don't share elements.</td></tr><tr><td><code>inherit</code></td><td><code>boolean | \"id\"</code></td><td>Whether to inherit the parent group's id. <code>false</code> isolates the group.</td></tr></tbody></table>\n<h2>Namespacing</h2>\n<p>When the same component is rendered more than once — an accordion, a list of cards — give each\r\ninstance its own <code>id</code> so their <code>layoutId</code>s stay distinct.</p>\n<pre><code class=\"language-svelte\">{#each groups as group (group.id)}\r\n\t&#x3C;LayoutGroup id={group.id}>\r\n\t\t&#x3C;Accordion items={group.items} />\r\n\t&#x3C;/LayoutGroup>\r\n{/each}\n</code></pre>\n<callout type=\"note\" title=\"Replaces AnimateSharedLayout\">\n<p><code>AnimateSharedLayout</code> was removed in <code>0.2</code>. Shared layout animations now work without a wrapper —\r\nreach for <code>LayoutGroup</code> when you need grouped re-measurement or namespacing.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport SharedLayoutDemo from \"$lib/components/demos/shared-layout-demo.svelte\";\r\n</script>\r\n\r\nComponents measure their own layout when they re-render. `LayoutGroup` makes a set of components\r\nre-measure together, so a change in one animates its siblings correctly — and it namespaces\r\n`layoutId` so shared layout animations don't collide.\r\n\r\n<DemoContainer class=\"flex justify-center py-10\">\r\n\t<SharedLayoutDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, LayoutGroup } from \"motion-start\";\r\n\r\n\tlet selected = $state(\"spring\");\r\n</script>\r\n\r\n<LayoutGroup>\r\n\t{#each tabs as tab (tab)}\r\n\t\t<button onclick={() => (selected = tab)}>\r\n\t\t\t{#if selected === tab}\r\n\t\t\t\t<motion.span layoutId=\"pill\" />\r\n\t\t\t{/if}\r\n\t\t\t{tab}\r\n\t\t</button>\r\n\t{/each}\r\n</LayoutGroup>\r\n```\r\n\r\n## Props\r\n\r\n| Prop      | Type                | Description                                                            |\r\n| --------- | ------------------- | ---------------------------------------------------------------------- |\r\n| `id`      | `string`            | Namespace for `layoutId`s within the group, so repeated groups don't share elements. |\r\n| `inherit` | `boolean \\| \"id\"`   | Whether to inherit the parent group's id. `false` isolates the group.  |\r\n\r\n## Namespacing\r\n\r\nWhen the same component is rendered more than once — an accordion, a list of cards — give each\r\ninstance its own `id` so their `layoutId`s stay distinct.\r\n\r\n```svelte\r\n{#each groups as group (group.id)}\r\n\t<LayoutGroup id={group.id}>\r\n\t\t<Accordion items={group.items} />\r\n\t</LayoutGroup>\r\n{/each}\r\n```\r\n\r\n<Callout type=\"note\" title=\"Replaces AnimateSharedLayout\">\r\n\r\n`AnimateSharedLayout` was removed in `0.2`. Shared layout animations now work without a wrapper —\r\nreach for `LayoutGroup` when you need grouped re-measurement or namespacing.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Props",
        "url": "#props",
        "items": []
      },
      {
        "title": "Namespacing",
        "url": "#namespacing",
        "items": []
      }
    ],
    "section": "Components",
    "slug": "components/layout-group",
    "slugFull": "/components/layout-group"
  },
  {
    "title": "LazyMotion",
    "description": "Code-split animation features to keep the initial bundle small.",
    "path": "components/04_lazy-motion",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<p>The full <code>motion</code> component bundles every feature. <code>LazyMotion</code> lets you ship the much smaller <code>m</code>\r\ncomponent and load only the features you need — synchronously or asynchronously.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { LazyMotion, domAnimation } from \"motion-start\";\r\n\timport { m } from \"motion-start/m\";\r\n&#x3C;/script>\r\n\r\n&#x3C;LazyMotion features={domAnimation}>\r\n\t&#x3C;m.div animate={{ opacity: 1 }} />\r\n&#x3C;/LazyMotion>\n</code></pre>\n<h2>Feature bundles</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Bundle</th><th>Contents</th></tr></thead><tbody><tr><td><code>domMin</code></td><td>Animations only.</td></tr><tr><td><code>domAnimation</code></td><td>Animations, variants, exit animations and hover/tap/focus gestures.</td></tr><tr><td><code>domMax</code></td><td>Everything, including pan, drag and layout animations.</td></tr></tbody></table>\n<h2>Loading asynchronously</h2>\n<p>Pass a function returning a promise to defer loading until after first paint.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { LazyMotion } from \"motion-start\";\r\n\timport { m } from \"motion-start/m\";\r\n\r\n\tconst loadFeatures = () => import(\"motion-start\").then((res) => res.domMax);\r\n&#x3C;/script>\r\n\r\n&#x3C;LazyMotion features={loadFeatures} strict>\r\n\t&#x3C;m.div animate={{ x: 100 }} />\r\n&#x3C;/LazyMotion>\n</code></pre>\n<h2>Props</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>features</code></td><td><code>FeatureBundle | () => Promise&#x3C;…></code></td><td>The bundle to load, directly or lazily.</td></tr><tr><td><code>strict</code></td><td><code>boolean</code></td><td>Throw if a full <code>motion</code> component renders inside, which would undo the code-splitting.</td></tr></tbody></table>\n<callout type=\"tip\" title=\"Use strict in development\">\n<p><code>strict</code> makes accidental <code>motion</code> imports inside a <code>LazyMotion</code> tree a loud error rather than a\r\nsilent bundle-size regression.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\nThe full `motion` component bundles every feature. `LazyMotion` lets you ship the much smaller `m`\r\ncomponent and load only the features you need — synchronously or asynchronously.\r\n\r\n```svelte\r\n<script>\r\n\timport { LazyMotion, domAnimation } from \"motion-start\";\r\n\timport { m } from \"motion-start/m\";\r\n</script>\r\n\r\n<LazyMotion features={domAnimation}>\r\n\t<m.div animate={{ opacity: 1 }} />\r\n</LazyMotion>\r\n```\r\n\r\n## Feature bundles\r\n\r\n| Bundle         | Contents                                                                |\r\n| -------------- | ----------------------------------------------------------------------- |\r\n| `domMin`       | Animations only.                                                        |\r\n| `domAnimation` | Animations, variants, exit animations and hover/tap/focus gestures.      |\r\n| `domMax`       | Everything, including pan, drag and layout animations.                   |\r\n\r\n## Loading asynchronously\r\n\r\nPass a function returning a promise to defer loading until after first paint.\r\n\r\n```svelte\r\n<script>\r\n\timport { LazyMotion } from \"motion-start\";\r\n\timport { m } from \"motion-start/m\";\r\n\r\n\tconst loadFeatures = () => import(\"motion-start\").then((res) => res.domMax);\r\n</script>\r\n\r\n<LazyMotion features={loadFeatures} strict>\r\n\t<m.div animate={{ x: 100 }} />\r\n</LazyMotion>\r\n```\r\n\r\n## Props\r\n\r\n| Prop       | Type                                  | Description                                                          |\r\n| ---------- | ------------------------------------- | -------------------------------------------------------------------- |\r\n| `features` | `FeatureBundle \\| () => Promise<…>`    | The bundle to load, directly or lazily.                              |\r\n| `strict`   | `boolean`                             | Throw if a full `motion` component renders inside, which would undo the code-splitting. |\r\n\r\n<Callout type=\"tip\" title=\"Use strict in development\">\r\n\r\n`strict` makes accidental `motion` imports inside a `LazyMotion` tree a loud error rather than a\r\nsilent bundle-size regression.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Feature bundles",
        "url": "#feature-bundles",
        "items": []
      },
      {
        "title": "Loading asynchronously",
        "url": "#loading-asynchronously",
        "items": []
      },
      {
        "title": "Props",
        "url": "#props",
        "items": []
      }
    ],
    "section": "Components",
    "slug": "components/lazy-motion",
    "slugFull": "/components/lazy-motion"
  },
  {
    "title": "MotionConfig",
    "description": "Set defaults for every motion component beneath it.",
    "path": "components/05_motion-config",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<p><code>MotionConfig</code> provides configuration to all descendant motion components — most usefully a default\r\ntransition and a reduced-motion policy.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { MotionConfig } from \"motion-start\";\r\n&#x3C;/script>\r\n\r\n&#x3C;MotionConfig transition={{ type: \"spring\", stiffness: 240, damping: 24 }}>\r\n\t{@render children()}\r\n&#x3C;/MotionConfig>\n</code></pre>\n<h2>Props</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>transition</code></td><td><code>Transition</code></td><td>Default transition for descendants that don't define their own.</td></tr><tr><td><code>reducedMotion</code></td><td><code>\"user\" | \"always\" | \"never\"</code></td><td>How to respond to the OS reduced-motion setting. <code>\"user\"</code> follows the system.</td></tr><tr><td><code>nonce</code></td><td><code>string</code></td><td>CSP nonce for injected styles.</td></tr><tr><td><code>isValidProp</code></td><td><code>(key: string) => boolean</code></td><td>Override which props are forwarded to the DOM element.</td></tr></tbody></table>\n<h2>Reduced motion</h2>\n<p>With <code>reducedMotion=\"user\"</code>, transform and layout animations are disabled when the user has asked\r\nfor reduced motion, while opacity and colour animations continue.</p>\n<pre><code class=\"language-svelte\">&#x3C;MotionConfig reducedMotion=\"user\">\r\n\t&#x3C;App />\r\n&#x3C;/MotionConfig>\n</code></pre>\n<callout type=\"tip\" title=\"Set it once\">\n<p>Put <code>MotionConfig</code> in your root layout so the whole app shares one motion language — then override\r\nper component only where it matters.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\n`MotionConfig` provides configuration to all descendant motion components — most usefully a default\r\ntransition and a reduced-motion policy.\r\n\r\n```svelte\r\n<script>\r\n\timport { MotionConfig } from \"motion-start\";\r\n</script>\r\n\r\n<MotionConfig transition={{ type: \"spring\", stiffness: 240, damping: 24 }}>\r\n\t{@render children()}\r\n</MotionConfig>\r\n```\r\n\r\n## Props\r\n\r\n| Prop            | Type                                     | Description                                                                 |\r\n| --------------- | ---------------------------------------- | --------------------------------------------------------------------------- |\r\n| `transition`    | `Transition`                             | Default transition for descendants that don't define their own.             |\r\n| `reducedMotion` | `\"user\" \\| \"always\" \\| \"never\"`           | How to respond to the OS reduced-motion setting. `\"user\"` follows the system. |\r\n| `nonce`         | `string`                                 | CSP nonce for injected styles.                                              |\r\n| `isValidProp`   | `(key: string) => boolean`               | Override which props are forwarded to the DOM element.                      |\r\n\r\n## Reduced motion\r\n\r\nWith `reducedMotion=\"user\"`, transform and layout animations are disabled when the user has asked\r\nfor reduced motion, while opacity and colour animations continue.\r\n\r\n```svelte\r\n<MotionConfig reducedMotion=\"user\">\r\n\t<App />\r\n</MotionConfig>\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Set it once\">\r\n\r\nPut `MotionConfig` in your root layout so the whole app shares one motion language — then override\r\nper component only where it matters.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Props",
        "url": "#props",
        "items": []
      },
      {
        "title": "Reduced motion",
        "url": "#reduced-motion",
        "items": []
      }
    ],
    "section": "Components",
    "slug": "components/motion-config",
    "slugFull": "/components/motion-config"
  },
  {
    "title": "Reorder",
    "description": "Drag-to-reorder lists with layout animations built in.",
    "path": "components/06_reorder",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport ReorderDemo from \"$lib/components/demos/reorder-demo.svelte\";\n</script>\n<p><code>Reorder.Group</code> and <code>Reorder.Item</code> turn any list into a drag-to-reorder list. Items are draggable,\r\nanimate to their new positions, and the group reports the new order.</p>\n<democontainer class=\"flex justify-center py-8\">\n\t<reorderdemo>\n</reorderdemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { Reorder } from \"motion-start\";\r\n\r\n\tlet items = $state([\"spring\", \"tween\", \"inertia\", \"keyframes\"]);\r\n&#x3C;/script>\r\n\r\n&#x3C;Reorder.Group values={items} onReorder={(next) => (items = next)}>\r\n\t{#snippet children({ item })}\r\n\t\t&#x3C;Reorder.Item value={item}>\r\n\t\t\t{item}\r\n\t\t&#x3C;/Reorder.Item>\r\n\t{/snippet}\r\n&#x3C;/Reorder.Group>\n</code></pre>\n<h2>Reorder.Group</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>values</code></td><td><code>T[]</code></td><td>The array being reordered. Required.</td></tr><tr><td><code>onReorder</code></td><td><code>(newOrder: T[]) => void</code></td><td>Called with the new order as items are dragged. Required.</td></tr><tr><td><code>axis</code></td><td><code>\"x\" | \"y\"</code></td><td>Drag axis. Defaults to <code>\"y\"</code>.</td></tr><tr><td><code>as</code></td><td><code>string</code></td><td>Element to render. Defaults to <code>\"ul\"</code>.</td></tr></tbody></table>\n<p>The group renders its children through a snippet that receives each item, so the list stays keyed by\r\nvalue.</p>\n<h2>Reorder.Item</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>value</code></td><td><code>T</code></td><td>The item's entry in <code>values</code>. Required.</td></tr><tr><td><code>as</code></td><td><code>string</code></td><td>Element to render. Defaults to <code>\"li\"</code>.</td></tr></tbody></table>\n<p><code>Reorder.Item</code> is a motion component, so <code>whileDrag</code>, <code>transition</code> and the other animation props all\r\nwork.</p>\n<pre><code class=\"language-svelte\">&#x3C;Reorder.Item value={item} whileDrag={{ scale: 1.04 }} transition={{ type: \"spring\" }}>\r\n\t{item}\r\n&#x3C;/Reorder.Item>\n</code></pre>\n<callout type=\"tip\" title=\"Drag handles\">\n<p>Set <code>dragListener={false}</code> on the item and start the drag from a handle with <code>useDragControls</code> when\r\nthe whole row shouldn't be draggable.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport ReorderDemo from \"$lib/components/demos/reorder-demo.svelte\";\r\n</script>\r\n\r\n`Reorder.Group` and `Reorder.Item` turn any list into a drag-to-reorder list. Items are draggable,\r\nanimate to their new positions, and the group reports the new order.\r\n\r\n<DemoContainer class=\"flex justify-center py-8\">\r\n\t<ReorderDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<script>\r\n\timport { Reorder } from \"motion-start\";\r\n\r\n\tlet items = $state([\"spring\", \"tween\", \"inertia\", \"keyframes\"]);\r\n</script>\r\n\r\n<Reorder.Group values={items} onReorder={(next) => (items = next)}>\r\n\t{#snippet children({ item })}\r\n\t\t<Reorder.Item value={item}>\r\n\t\t\t{item}\r\n\t\t</Reorder.Item>\r\n\t{/snippet}\r\n</Reorder.Group>\r\n```\r\n\r\n## Reorder.Group\r\n\r\n| Prop         | Type                        | Description                                                        |\r\n| ------------ | --------------------------- | ------------------------------------------------------------------ |\r\n| `values`     | `T[]`                       | The array being reordered. Required.                               |\r\n| `onReorder`  | `(newOrder: T[]) => void`   | Called with the new order as items are dragged. Required.          |\r\n| `axis`       | `\"x\" \\| \"y\"`                 | Drag axis. Defaults to `\"y\"`.                                      |\r\n| `as`         | `string`                    | Element to render. Defaults to `\"ul\"`.                             |\r\n\r\nThe group renders its children through a snippet that receives each item, so the list stays keyed by\r\nvalue.\r\n\r\n## Reorder.Item\r\n\r\n| Prop    | Type     | Description                                                    |\r\n| ------- | -------- | -------------------------------------------------------------- |\r\n| `value` | `T`      | The item's entry in `values`. Required.                        |\r\n| `as`    | `string` | Element to render. Defaults to `\"li\"`.                         |\r\n\r\n`Reorder.Item` is a motion component, so `whileDrag`, `transition` and the other animation props all\r\nwork.\r\n\r\n```svelte\r\n<Reorder.Item value={item} whileDrag={{ scale: 1.04 }} transition={{ type: \"spring\" }}>\r\n\t{item}\r\n</Reorder.Item>\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Drag handles\">\r\n\r\nSet `dragListener={false}` on the item and start the drag from a handle with `useDragControls` when\r\nthe whole row shouldn't be draggable.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Reorder.Group",
        "url": "#reordergroup",
        "items": []
      },
      {
        "title": "Reorder.Item",
        "url": "#reorderitem",
        "items": []
      }
    ],
    "section": "Components",
    "slug": "components/reorder",
    "slugFull": "/components/reorder"
  },
  {
    "title": "Overview",
    "description": "Track and compose animating values outside of Svelte state.",
    "path": "motion-values/01_overview",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\n</script>\n<p>Motion components create motion values internally for every animating value. Creating them yourself\r\nlets you read velocity, chain values together, and drive animations without triggering a re-render.</p>\n<democontainer class=\"py-8\">\n\t<motionvaluedemo>\n</motionvaluedemo></democontainer>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useMotionValue, useTransform } from \"motion-start\";\r\n\r\n\tconst x = useMotionValue(0);\r\n\tconst rotate = useTransform(x, [-140, 140], [-24, 24]);\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div drag=\"x\" style={{ x, rotate }} />\n</code></pre>\n<h2>The MotionValue API</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Description</th></tr></thead><tbody><tr><td><code>get()</code></td><td>The current value.</td></tr><tr><td><code>set(v)</code></td><td>Set the value, without animating.</td></tr><tr><td><code>jump(v)</code></td><td>Set the value and reset velocity.</td></tr><tr><td><code>getVelocity()</code></td><td>Velocity per second.</td></tr><tr><td><code>on(\"change\", cb)</code></td><td>Subscribe to changes. Returns an unsubscribe function.</td></tr><tr><td><code>isAnimating()</code></td><td>Whether an animation is currently driving the value.</td></tr><tr><td><code>stop()</code></td><td>Stop the active animation.</td></tr></tbody></table>\n<p>Because updates bypass Svelte's reactivity, changing a motion value re-renders nothing — it writes\r\nstraight to the DOM on the next frame.</p>\n<h2>Available hooks</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Hook</th><th>Description</th></tr></thead><tbody><tr><td><code>useMotionValue</code></td><td>Create a motion value.</td></tr><tr><td><code>useTransform</code></td><td>Map one or more values into a new one.</td></tr><tr><td><code>useSpring</code></td><td>A value that springs towards its target.</td></tr><tr><td><code>useMotionTemplate</code></td><td>Compose values into a string, e.g. a <code>filter</code> or <code>background</code>.</td></tr><tr><td><code>useScroll</code></td><td>Scroll offset and progress values.</td></tr><tr><td><code>useTime</code></td><td>Milliseconds elapsed, updated every frame.</td></tr><tr><td><code>useVelocity</code></td><td>The velocity of another motion value.</td></tr><tr><td><code>useMotionValueEvent</code></td><td>Subscribe to a motion value event, with automatic cleanup.</td></tr><tr><td><code>useWillChange</code></td><td>A <code>will-change</code> string that tracks the values being animated.</td></tr></tbody></table>\n<callout type=\"note\" title=\"Passing values in\">\n<p>Motion values are passed to a component through <code>style</code>. Transform shorthands like <code>x</code>, <code>rotate</code> and\r\n<code>scale</code> accept them directly, as do regular CSS properties.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\r\n</script>\r\n\r\nMotion components create motion values internally for every animating value. Creating them yourself\r\nlets you read velocity, chain values together, and drive animations without triggering a re-render.\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<MotionValueDemo />\r\n</DemoContainer>\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useMotionValue, useTransform } from \"motion-start\";\r\n\r\n\tconst x = useMotionValue(0);\r\n\tconst rotate = useTransform(x, [-140, 140], [-24, 24]);\r\n</script>\r\n\r\n<motion.div drag=\"x\" style={{ x, rotate }} />\r\n```\r\n\r\n## The MotionValue API\r\n\r\n| Method              | Description                                                     |\r\n| ------------------- | ---------------------------------------------------------------- |\r\n| `get()`             | The current value.                                              |\r\n| `set(v)`            | Set the value, without animating.                               |\r\n| `jump(v)`           | Set the value and reset velocity.                               |\r\n| `getVelocity()`     | Velocity per second.                                            |\r\n| `on(\"change\", cb)`  | Subscribe to changes. Returns an unsubscribe function.          |\r\n| `isAnimating()`     | Whether an animation is currently driving the value.            |\r\n| `stop()`            | Stop the active animation.                                      |\r\n\r\nBecause updates bypass Svelte's reactivity, changing a motion value re-renders nothing — it writes\r\nstraight to the DOM on the next frame.\r\n\r\n## Available hooks\r\n\r\n| Hook                    | Description                                                             |\r\n| ----------------------- | ------------------------------------------------------------------------ |\r\n| `useMotionValue`        | Create a motion value.                                                  |\r\n| `useTransform`          | Map one or more values into a new one.                                   |\r\n| `useSpring`             | A value that springs towards its target.                                 |\r\n| `useMotionTemplate`     | Compose values into a string, e.g. a `filter` or `background`.           |\r\n| `useScroll`             | Scroll offset and progress values.                                       |\r\n| `useTime`               | Milliseconds elapsed, updated every frame.                               |\r\n| `useVelocity`           | The velocity of another motion value.                                    |\r\n| `useMotionValueEvent`   | Subscribe to a motion value event, with automatic cleanup.               |\r\n| `useWillChange`         | A `will-change` string that tracks the values being animated.            |\r\n\r\n<Callout type=\"note\" title=\"Passing values in\">\r\n\r\nMotion values are passed to a component through `style`. Transform shorthands like `x`, `rotate` and\r\n`scale` accept them directly, as do regular CSS properties.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "The MotionValue API",
        "url": "#the-motionvalue-api",
        "items": []
      },
      {
        "title": "Available hooks",
        "url": "#available-hooks",
        "items": []
      }
    ],
    "section": "Motion values",
    "slug": "motion-values/overview",
    "slugFull": "/motion-values/overview"
  },
  {
    "title": "useTransform",
    "description": "Map one or more motion values into a new one.",
    "path": "motion-values/02_use-transform",
    "content": "<script>\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\n\timport MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\n</script>\n<p><code>useTransform</code> creates a motion value derived from others. It has two forms: range mapping and a\r\ntransformer function.</p>\n<democontainer class=\"py-8\">\n\t<motionvaluedemo>\n</motionvaluedemo></democontainer>\n<h2>Range mapping</h2>\n<pre><code class=\"language-ts\">const x = useMotionValue(0);\r\nconst opacity = useTransform(x, [-200, 0, 200], [0, 1, 0]);\n</code></pre>\n<p>The input range must be a linear series — all increasing or all decreasing. The output range can be\r\nnumbers, colours, shadows or any other value type Motion Start can interpolate, as long as every\r\nentry is the same type and format.</p>\n<pre><code class=\"language-ts\">const background = useTransform(\r\n\tx,\r\n\t[-140, 0, 140],\r\n\t[\"#e11d48\", \"#a855f7\", \"#10b981\"]\r\n);\n</code></pre>\n<p>Options are passed as a fourth argument:</p>\n<pre><code class=\"language-ts\">const opacity = useTransform(x, [0, 100], [0, 1], { clamp: false, ease: easeOut });\n</code></pre>\n<h2>Transformer functions</h2>\n<pre><code class=\"language-ts\">const time = useTime();\r\nconst wobble = useTransform(time, (t) => Math.sin(t / 1000) * 10);\n</code></pre>\n<p>Multiple inputs are supported — the transformer receives an array:</p>\n<pre><code class=\"language-ts\">const x = useMotionValue(0);\r\nconst y = useMotionValue(0);\r\nconst distance = useTransform([x, y], ([latestX, latestY]) =>\r\n\tMath.hypot(latestX, latestY)\r\n);\n</code></pre>\n<callout type=\"tip\" title=\"Chain freely\">\n<p>Transformed values are themselves motion values, so they can feed <code>useSpring</code>, <code>useMotionTemplate</code>\r\nor another <code>useTransform</code>.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout, DemoContainer } from \"@svecodocs/kit\";\r\n\timport MotionValueDemo from \"$lib/components/demos/motion-value-demo.svelte\";\r\n</script>\r\n\r\n`useTransform` creates a motion value derived from others. It has two forms: range mapping and a\r\ntransformer function.\r\n\r\n<DemoContainer class=\"py-8\">\r\n\t<MotionValueDemo />\r\n</DemoContainer>\r\n\r\n## Range mapping\r\n\r\n```ts\r\nconst x = useMotionValue(0);\r\nconst opacity = useTransform(x, [-200, 0, 200], [0, 1, 0]);\r\n```\r\n\r\nThe input range must be a linear series — all increasing or all decreasing. The output range can be\r\nnumbers, colours, shadows or any other value type Motion Start can interpolate, as long as every\r\nentry is the same type and format.\r\n\r\n```ts\r\nconst background = useTransform(\r\n\tx,\r\n\t[-140, 0, 140],\r\n\t[\"#e11d48\", \"#a855f7\", \"#10b981\"]\r\n);\r\n```\r\n\r\nOptions are passed as a fourth argument:\r\n\r\n```ts\r\nconst opacity = useTransform(x, [0, 100], [0, 1], { clamp: false, ease: easeOut });\r\n```\r\n\r\n## Transformer functions\r\n\r\n```ts\r\nconst time = useTime();\r\nconst wobble = useTransform(time, (t) => Math.sin(t / 1000) * 10);\r\n```\r\n\r\nMultiple inputs are supported — the transformer receives an array:\r\n\r\n```ts\r\nconst x = useMotionValue(0);\r\nconst y = useMotionValue(0);\r\nconst distance = useTransform([x, y], ([latestX, latestY]) =>\r\n\tMath.hypot(latestX, latestY)\r\n);\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Chain freely\">\r\n\r\nTransformed values are themselves motion values, so they can feed `useSpring`, `useMotionTemplate`\r\nor another `useTransform`.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Range mapping",
        "url": "#range-mapping",
        "items": []
      },
      {
        "title": "Transformer functions",
        "url": "#transformer-functions",
        "items": []
      }
    ],
    "section": "Motion values",
    "slug": "motion-values/use-transform",
    "slugFull": "/motion-values/use-transform"
  },
  {
    "title": "useSpring",
    "description": "A motion value that springs towards its target.",
    "path": "motion-values/03_use-spring",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<p><code>useSpring</code> creates a motion value that animates towards whatever it is set to, using spring physics.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useSpring } from \"motion-start\";\r\n\r\n\tconst x = useSpring(0, { stiffness: 300, damping: 30 });\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div style={{ x }} onclick={() => x.set(200)} />\n</code></pre>\n<h2>Following another value</h2>\n<p>Pass a motion value as the first argument and the spring will follow it — the classic way to smooth\r\nout a scroll or pointer value.</p>\n<pre><code class=\"language-ts\">const { scrollYProgress } = useScroll();\r\nconst smoothProgress = useSpring(scrollYProgress, { stiffness: 120, damping: 30 });\n</code></pre>\n<h2>Options</h2>\n<p>Any <a href=\"/docs/animation/transitions#spring\">spring transition option</a> is accepted: <code>stiffness</code>,\r\n<code>damping</code>, <code>mass</code>, <code>bounce</code>, <code>duration</code>, <code>restSpeed</code> and <code>restDelta</code>.</p>\n<pre><code class=\"language-ts\">const gentle = useSpring(0, { bounce: 0.1, duration: 0.6 });\n</code></pre>\n<callout type=\"tip\" title=\"Jumping without animating\">\n<p>Use <code>spring.jump(value)</code> to move to a value instantly and reset velocity — useful when resetting\r\nstate between interactions.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\n`useSpring` creates a motion value that animates towards whatever it is set to, using spring physics.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useSpring } from \"motion-start\";\r\n\r\n\tconst x = useSpring(0, { stiffness: 300, damping: 30 });\r\n</script>\r\n\r\n<motion.div style={{ x }} onclick={() => x.set(200)} />\r\n```\r\n\r\n## Following another value\r\n\r\nPass a motion value as the first argument and the spring will follow it — the classic way to smooth\r\nout a scroll or pointer value.\r\n\r\n```ts\r\nconst { scrollYProgress } = useScroll();\r\nconst smoothProgress = useSpring(scrollYProgress, { stiffness: 120, damping: 30 });\r\n```\r\n\r\n## Options\r\n\r\nAny [spring transition option](/docs/animation/transitions#spring) is accepted: `stiffness`,\r\n`damping`, `mass`, `bounce`, `duration`, `restSpeed` and `restDelta`.\r\n\r\n```ts\r\nconst gentle = useSpring(0, { bounce: 0.1, duration: 0.6 });\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Jumping without animating\">\r\n\r\nUse `spring.jump(value)` to move to a value instantly and reset velocity — useful when resetting\r\nstate between interactions.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Following another value",
        "url": "#following-another-value",
        "items": []
      },
      {
        "title": "Options",
        "url": "#options",
        "items": []
      }
    ],
    "section": "Motion values",
    "slug": "motion-values/use-spring",
    "slugFull": "/motion-values/use-spring"
  },
  {
    "title": "useScroll",
    "description": "Motion values that track scroll offset and progress.",
    "path": "motion-values/04_use-scroll",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<p><code>useScroll</code> returns four motion values that update as the page, a container, or a target element\r\nscrolls.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useScroll } from \"motion-start\";\r\n\r\n\tconst { scrollYProgress } = useScroll();\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div class=\"progress\" style={{ scaleX: scrollYProgress, transformOrigin: \"0%\" }} />\n</code></pre>\n<h2>Returned values</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>scrollX</code></td><td>Horizontal offset in pixels.</td></tr><tr><td><code>scrollY</code></td><td>Vertical offset in pixels.</td></tr><tr><td><code>scrollXProgress</code></td><td>Horizontal progress, <code>0</code>–<code>1</code>.</td></tr><tr><td><code>scrollYProgress</code></td><td>Vertical progress, <code>0</code>–<code>1</code>.</td></tr></tbody></table>\n<h2>Options</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Option</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>container</code></td><td><code>RefObject&#x3C;HTMLElement></code></td><td>Scrollable element to track instead of the window.</td></tr><tr><td><code>target</code></td><td><code>RefObject&#x3C;Element></code></td><td>Element whose position within the scroll container is tracked.</td></tr><tr><td><code>offset</code></td><td><code>ScrollOffset</code></td><td>When tracking starts and ends, e.g. <code>[\"start end\", \"end start\"]</code>.</td></tr><tr><td><code>axis</code></td><td><code>\"x\" | \"y\"</code></td><td>Axis for <code>offset</code> calculations. Defaults to <code>\"y\"</code>.</td></tr><tr><td><code>layoutEffect</code></td><td><code>boolean</code></td><td>Set up before paint (<code>true</code>, the default) or after.</td></tr></tbody></table>\n<h2>Tracking an element</h2>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useScroll, useTransform } from \"motion-start\";\r\n\r\n\tconst ref = { current: null };\r\n\tconst { scrollYProgress } = useScroll({ target: ref, offset: [\"start end\", \"end start\"] });\r\n\tconst y = useTransform(scrollYProgress, [0, 1], [\"-20%\", \"20%\"]);\r\n&#x3C;/script>\r\n\r\n&#x3C;section bind:this={ref.current}>\r\n\t&#x3C;motion.img style={{ y }} src=\"/parallax.jpg\" alt=\"\" />\r\n&#x3C;/section>\n</code></pre>\n<p>Offsets are pairs of intersection points, where the first word refers to the target and the second\r\nto the container: <code>\"start end\"</code> means \"the start of the target meets the end of the container\".</p>\n<callout type=\"warning\" title=\"Ref hydration\">\n<p>Pass a ref object created in the same component as the element. If <code>useScroll</code> runs before the ref\r\nis bound, set <code>layoutEffect: false</code> so setup happens after the DOM is ready.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\n`useScroll` returns four motion values that update as the page, a container, or a target element\r\nscrolls.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useScroll } from \"motion-start\";\r\n\r\n\tconst { scrollYProgress } = useScroll();\r\n</script>\r\n\r\n<motion.div class=\"progress\" style={{ scaleX: scrollYProgress, transformOrigin: \"0%\" }} />\r\n```\r\n\r\n## Returned values\r\n\r\n| Value             | Description                    |\r\n| ----------------- | ------------------------------ |\r\n| `scrollX`         | Horizontal offset in pixels.   |\r\n| `scrollY`         | Vertical offset in pixels.     |\r\n| `scrollXProgress` | Horizontal progress, `0`–`1`.  |\r\n| `scrollYProgress` | Vertical progress, `0`–`1`.    |\r\n\r\n## Options\r\n\r\n| Option         | Type                  | Description                                                            |\r\n| -------------- | --------------------- | ---------------------------------------------------------------------- |\r\n| `container`    | `RefObject<HTMLElement>` | Scrollable element to track instead of the window.                  |\r\n| `target`       | `RefObject<Element>`  | Element whose position within the scroll container is tracked.          |\r\n| `offset`       | `ScrollOffset`        | When tracking starts and ends, e.g. `[\"start end\", \"end start\"]`.       |\r\n| `axis`         | `\"x\" \\| \"y\"`           | Axis for `offset` calculations. Defaults to `\"y\"`.                     |\r\n| `layoutEffect` | `boolean`             | Set up before paint (`true`, the default) or after.                    |\r\n\r\n## Tracking an element\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useScroll, useTransform } from \"motion-start\";\r\n\r\n\tconst ref = { current: null };\r\n\tconst { scrollYProgress } = useScroll({ target: ref, offset: [\"start end\", \"end start\"] });\r\n\tconst y = useTransform(scrollYProgress, [0, 1], [\"-20%\", \"20%\"]);\r\n</script>\r\n\r\n<section bind:this={ref.current}>\r\n\t<motion.img style={{ y }} src=\"/parallax.jpg\" alt=\"\" />\r\n</section>\r\n```\r\n\r\nOffsets are pairs of intersection points, where the first word refers to the target and the second\r\nto the container: `\"start end\"` means \"the start of the target meets the end of the container\".\r\n\r\n<Callout type=\"warning\" title=\"Ref hydration\">\r\n\r\nPass a ref object created in the same component as the element. If `useScroll` runs before the ref\r\nis bound, set `layoutEffect: false` so setup happens after the DOM is ready.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Returned values",
        "url": "#returned-values",
        "items": []
      },
      {
        "title": "Options",
        "url": "#options",
        "items": []
      },
      {
        "title": "Tracking an element",
        "url": "#tracking-an-element",
        "items": []
      }
    ],
    "section": "Motion values",
    "slug": "motion-values/use-scroll",
    "slugFull": "/motion-values/use-scroll"
  },
  {
    "title": "useTime & useVelocity",
    "description": "Frame-driven time and the velocity of any motion value.",
    "path": "motion-values/05_use-time-velocity",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<h2>useTime</h2>\n<p><code>useTime</code> returns a motion value that updates every frame with the milliseconds elapsed since it was\r\ncreated. It's the simplest way to drive a perpetual animation.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useTime, useTransform } from \"motion-start\";\r\n\r\n\tconst time = useTime();\r\n\tconst rotate = useTransform(time, [0, 4000], [0, 360], { clamp: false });\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div style={{ rotate }} />\n</code></pre>\n<h2>useVelocity</h2>\n<p><code>useVelocity</code> tracks the velocity of another motion value, in units per second.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useScroll, useVelocity, useTransform } from \"motion-start\";\r\n\r\n\tconst { scrollY } = useScroll();\r\n\tconst scrollVelocity = useVelocity(scrollY);\r\n\tconst skewY = useTransform(scrollVelocity, [-1000, 1000], [-5, 5], { clamp: true });\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div style={{ skewY }} />\n</code></pre>\n<p>Velocity is itself a motion value, so it can be smoothed with <code>useSpring</code> before use:</p>\n<pre><code class=\"language-ts\">const smoothVelocity = useSpring(scrollVelocity, { stiffness: 400, damping: 40 });\n</code></pre>\n<callout type=\"tip\" title=\"Acceleration\">\n<p><code>useVelocity</code> can be applied to a velocity value to get acceleration — handy for elastic,\r\nphysical-feeling UI.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\n## useTime\r\n\r\n`useTime` returns a motion value that updates every frame with the milliseconds elapsed since it was\r\ncreated. It's the simplest way to drive a perpetual animation.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useTime, useTransform } from \"motion-start\";\r\n\r\n\tconst time = useTime();\r\n\tconst rotate = useTransform(time, [0, 4000], [0, 360], { clamp: false });\r\n</script>\r\n\r\n<motion.div style={{ rotate }} />\r\n```\r\n\r\n## useVelocity\r\n\r\n`useVelocity` tracks the velocity of another motion value, in units per second.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useScroll, useVelocity, useTransform } from \"motion-start\";\r\n\r\n\tconst { scrollY } = useScroll();\r\n\tconst scrollVelocity = useVelocity(scrollY);\r\n\tconst skewY = useTransform(scrollVelocity, [-1000, 1000], [-5, 5], { clamp: true });\r\n</script>\r\n\r\n<motion.div style={{ skewY }} />\r\n```\r\n\r\nVelocity is itself a motion value, so it can be smoothed with `useSpring` before use:\r\n\r\n```ts\r\nconst smoothVelocity = useSpring(scrollVelocity, { stiffness: 400, damping: 40 });\r\n```\r\n\r\n<Callout type=\"tip\" title=\"Acceleration\">\r\n\r\n`useVelocity` can be applied to a velocity value to get acceleration — handy for elastic,\r\nphysical-feeling UI.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "useTime",
        "url": "#usetime",
        "items": []
      },
      {
        "title": "useVelocity",
        "url": "#usevelocity",
        "items": []
      }
    ],
    "section": "Motion values",
    "slug": "motion-values/use-time-velocity",
    "slugFull": "/motion-values/use-time-velocity"
  },
  {
    "title": "useMotionTemplate",
    "description": "Compose motion values into a single string value.",
    "path": "motion-values/06_use-motion-template",
    "content": "<p>Some CSS properties take a string built from several values — <code>filter</code>, <code>background</code>, <code>clip-path</code>,\r\n<code>box-shadow</code>. <code>useMotionTemplate</code> builds that string from motion values as a tagged template, and\r\nreturns a new motion value that updates whenever any input changes.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useMotionValue, useMotionTemplate } from \"motion-start\";\r\n\r\n\tconst blur = useMotionValue(0);\r\n\tconst brightness = useMotionValue(1);\r\n\tconst filter = useMotionTemplate`blur(${blur}px) brightness(${brightness})`;\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.img style={{ filter }} src=\"/photo.jpg\" alt=\"\" />\n</code></pre>\n<p>It composes with <code>useTransform</code>, so any derived value can be interpolated in:</p>\n<pre><code class=\"language-ts\">const { scrollYProgress } = useScroll();\r\nconst spread = useTransform(scrollYProgress, [0, 1], [0, 40]);\r\nconst boxShadow = useMotionTemplate`0 0 ${spread}px rgba(168, 85, 247, 0.6)`;\n</code></pre>\n<p>Static text and numbers can be mixed in freely — only the interpolated motion values are tracked.</p>",
    "raw": "Some CSS properties take a string built from several values — `filter`, `background`, `clip-path`,\r\n`box-shadow`. `useMotionTemplate` builds that string from motion values as a tagged template, and\r\nreturns a new motion value that updates whenever any input changes.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useMotionValue, useMotionTemplate } from \"motion-start\";\r\n\r\n\tconst blur = useMotionValue(0);\r\n\tconst brightness = useMotionValue(1);\r\n\tconst filter = useMotionTemplate`blur(${blur}px) brightness(${brightness})`;\r\n</script>\r\n\r\n<motion.img style={{ filter }} src=\"/photo.jpg\" alt=\"\" />\r\n```\r\n\r\nIt composes with `useTransform`, so any derived value can be interpolated in:\r\n\r\n```ts\r\nconst { scrollYProgress } = useScroll();\r\nconst spread = useTransform(scrollYProgress, [0, 1], [0, 40]);\r\nconst boxShadow = useMotionTemplate`0 0 ${spread}px rgba(168, 85, 247, 0.6)`;\r\n```\r\n\r\nStatic text and numbers can be mixed in freely — only the interpolated motion values are tracked.",
    "toc": [],
    "section": "Motion values",
    "slug": "motion-values/use-motion-template",
    "slugFull": "/motion-values/use-motion-template"
  },
  {
    "title": "useMotionValueEvent",
    "description": "Subscribe to motion value events with automatic cleanup.",
    "path": "motion-values/07_use-motion-value-event",
    "content": "<script>\n\timport { Callout } from \"@svecodocs/kit\";\n</script>\n<p>Motion values emit events. <code>useMotionValueEvent</code> subscribes to one and unsubscribes automatically\r\nwhen the component is destroyed.</p>\n<pre><code class=\"language-svelte\">&#x3C;script>\r\n\timport { motion, useMotionValue, useMotionValueEvent } from \"motion-start\";\r\n\r\n\tconst x = useMotionValue(0);\r\n\r\n\tuseMotionValueEvent(x, \"change\", (latest) => {\r\n\t\tconsole.log(latest);\r\n\t});\r\n&#x3C;/script>\r\n\r\n&#x3C;motion.div drag=\"x\" style={{ x }} />\n</code></pre>\n<h2>Events</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Event</th><th>Description</th></tr></thead><tbody><tr><td><code>change</code></td><td>The value changed. Receives the latest value.</td></tr><tr><td><code>animationStart</code></td><td>An animation started driving the value.</td></tr><tr><td><code>animationComplete</code></td><td>The animation finished.</td></tr><tr><td><code>animationCancel</code></td><td>The animation was interrupted.</td></tr></tbody></table>\n<h2>Without the hook</h2>\n<p>Outside of a component, subscribe directly and clean up yourself:</p>\n<pre><code class=\"language-ts\">const unsubscribe = x.on(\"change\", (latest) => console.log(latest));\n</code></pre>\n<callout type=\"note\" title=\"No re-renders\">\n<p>Motion value changes don't trigger Svelte reactivity. If you need a change to update the UI, copy it\r\ninto <code>$state</code> inside the handler — but prefer driving the DOM with motion values where you can.</p>\n</callout>",
    "raw": "<script>\r\n\timport { Callout } from \"@svecodocs/kit\";\r\n</script>\r\n\r\nMotion values emit events. `useMotionValueEvent` subscribes to one and unsubscribes automatically\r\nwhen the component is destroyed.\r\n\r\n```svelte\r\n<script>\r\n\timport { motion, useMotionValue, useMotionValueEvent } from \"motion-start\";\r\n\r\n\tconst x = useMotionValue(0);\r\n\r\n\tuseMotionValueEvent(x, \"change\", (latest) => {\r\n\t\tconsole.log(latest);\r\n\t});\r\n</script>\r\n\r\n<motion.div drag=\"x\" style={{ x }} />\r\n```\r\n\r\n## Events\r\n\r\n| Event               | Description                                          |\r\n| ------------------- | ---------------------------------------------------- |\r\n| `change`            | The value changed. Receives the latest value.        |\r\n| `animationStart`    | An animation started driving the value.              |\r\n| `animationComplete` | The animation finished.                              |\r\n| `animationCancel`   | The animation was interrupted.                       |\r\n\r\n## Without the hook\r\n\r\nOutside of a component, subscribe directly and clean up yourself:\r\n\r\n```ts\r\nconst unsubscribe = x.on(\"change\", (latest) => console.log(latest));\r\n```\r\n\r\n<Callout type=\"note\" title=\"No re-renders\">\r\n\r\nMotion value changes don't trigger Svelte reactivity. If you need a change to update the UI, copy it\r\ninto `$state` inside the handler — but prefer driving the DOM with motion values where you can.\r\n\r\n</Callout>",
    "toc": [
      {
        "title": "Events",
        "url": "#events",
        "items": []
      },
      {
        "title": "Without the hook",
        "url": "#without-the-hook",
        "items": []
      }
    ],
    "section": "Motion values",
    "slug": "motion-values/use-motion-value-event",
    "slugFull": "/motion-values/use-motion-value-event"
  }
]